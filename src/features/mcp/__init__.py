"""MCP (Model Context Protocol) support: per-user tokens that let an external
MCP client act as the token's owner against the same tool surface chat uses.
See docs/plugin-api.md's tool registry section and src.features.llm.tools for
the tool machinery this reuses.
"""
