"""The single source of truth for PotionUI's own release version.

Not a preset/plugin/provider version — those are independently versioned
artifacts with their own `version:`/`version=` fields.
"""

POTIONUI_VERSION = "0.0.1"
