// Well-known model_metadata attribute keys. Mirrors WellKnownModelAttribute in
// src/features/models/attributes/well_known.py.
export const LORA_STRENGTH_KEY = 'strength';

// Trigger words are the built-in system `tags` attribute, keyed the same way
// as any other attribute definition rather than a dedicated model field.
export const TRIGGERS_KEY = 'triggers';
