---
title: Preset Context Cheat Sheet
category: Presets / Models
category_order: 70
order: 13
---

# Preset Context Cheat Sheet

Generated by `scripts/pipes_reference.py` from the live templating/preset surface. Regenerate with `python scripts/pipes_reference.py`; `--check` (wired into `scripts/docs_lint.py`) fails the build when this file drifts from the code. See the [Preset Authoring Guide](presets.md#template-contexts) for the narrative version of this reference.

## Globals, filters and template context roots

The Jinja globals/filters below are registered in `src/platform/templating/processor.py`; the `Template Context` entries are the `pipeline.yml` context roots built by `src/features/presets/processor.py`. Descriptions and signatures come straight from `src.features.developer.template_functions_documenter.TemplateFunctionsDocumenter` - the same source Help → Documentation → Template Functions serves.

### Path Helpers

**`path`** (alias `get_path_for`) — `path(path_type: str, file_name: str = None) -> str`

Resolve a filesystem path by resource type (checkpoint, lora, embedding, upscaler, detector, wildcard, diffusion_model, controlnet, std, ...). With no file_name, returns the resource directory; with one, the full path to that file. Plugins can register additional path types via the resolve_path hook.

- `{{ path('checkpoint') }}` → models/checkpoints
- `{{ path('lora', 'detail_enhancer.safetensors') }}` → models/loras/detail_enhancer.safetensors
- `{{ path('wildcard') }}` → models/wildcards

### Icon Mapping

**`icon`** (alias `get_icon`) — `icon(icon_type: str) -> str`

Map a semantic icon type to the frontend icon identifier (e.g. 'prompt', 'lora', 'controlnet', 'advanced', 'generation'). Used in form tab/section configuration.

- `{{ icon('prompt') }}` → 'pencil-square'
- `{{ icon('lora') }}` → 'puzzle-piece'
- `{{ icon('generation') }}` → 'bolt'

### Speed Profiles

**`get_speed_profile`** — `get_speed_profile(profile_name: str, default: Any = <required>) -> dict`

Look up a named entry from preset.yml's `speed_profiles:` block (draft/standard/max, ...) and return its dict of generation-knob overrides. Omitting `default` makes a missing profile a build error that names both the preset and the profile. `preset.speed_profiles.<name>` direct dot access is equivalent.

- `{{ get_speed_profile('draft')['steps'] }}` → 6 (from speed_profiles.draft.steps)
- `{{ preset.speed_profiles.draft.steps }}` → 6 (equivalent direct access)

### Filters

**`matches`** (alias `regex_search`) — `value | matches(pattern: str) -> bool`

Regex-search filter: True if `pattern` matches anywhere in the piped string value.

- `{% if form.model | matches('flux') %}...{% endif %}` → True if the selected model name contains 'flux'
- `{% if preset.name | matches('^SDXL') %}...{% endif %}` → True if the preset name starts with 'SDXL'

**`active_loras`** — `value | active_loras -> list`

Drop the lora_picker list's zero-strength entries. A missing strength (lora_picker's own strength_default applies), a negative strength (inverted LoRA) and a non-numeric strength all stay - only an exact-zero strength is dropped.

- `{{ form.loras | default([]) | active_loras }}` → The list with zero-strength entries removed

**`strip_model_dir`** — `value | strip_model_dir -> str`

Strip a model picker value down to its path relative to the depot type directory (`models/<checkpoints|loras|vae|...>/`), keeping any subdirectories underneath. A bare filename or an already backend-native ref (no such prefix) passes through unchanged; None/'' map to ''. Replaces the old `replace('models/loras/', '')` idiom in ComfyUI preset templates - see docs/models.md.

- `{{ form.vae | strip_model_dir }}` → 'x.safetensors' for 'models/vae/x.safetensors'
- `{{ item.model | strip_model_dir }}` → 'style/x.safetensors' for 'models/loras/style/x.safetensors'

**`default`** — `value | default(fallback: Any, boolean: bool = False) -> Any`

Jinja's builtin default filter - the ONLY way to tolerate a missing value. The environment uses StrictUndefined, so any reference to a field/key that wasn't provided RAISES a build error unless it is guarded by `| default(...)`. Use it on every optional form field.

- `{{ form.steps | default(30) }}` → form.steps if provided, else 30
- `{{ form.cfg | default(preset.vars.default_cfg) }}` → form.cfg or the preset's default_cfg var

### Template Context

**`form`** — `form.<field_name> -> native value`

The bound form's values (see bind_form / docs/presets.md). Each field resolves to its native, typed value - form.steps is an int, form.enabled a bool, form.loras a list, form.resolution a str. A reference to a field the form didn't provide raises unless guarded by `| default(...)`.

- `{{ form.steps | default(30) }}` → Number of steps (int)
- `{{ form.loras | default([]) }}` → List of selected LoRAs
- `{% if form.enable_controlnet | default(false) %}...{% endif %}` → Bool gate

**`request`** — `request.{mode, form_name}`

The active request: which mode and which form variant is being generated.

- `{{ request.mode }}` → 'txt2img'
- `{{ request.form_name }}` → 'custom'

**`generation`** — `generation.{prompts:{first,pairs,positives,negatives}, seed, quantity}`

Generation-level data resolved before the pipeline builds: expanded prompt pairs (one per image), the resolved seed (never -1 here), and the image quantity. `prompts.first` is the first pair; `prompts.pairs` the full list; `prompts.positives`/`negatives` the flattened sides.

- `{{ generation.prompts.first.positive }}` → First image's positive prompt
- `{{ generation.prompts.pairs }}` → List of {positive, negative} pairs

**`preset`** — `preset.{id, name, vars, speed_profiles, configuration}`

The preset manifest: its id/name, the `vars:` bag (preset-wide constants), the `speed_profiles:` block, and admin-set `configuration:` values.

- `{{ preset.vars.default_cfg }}` → The preset's default CFG constant
- `{{ preset.speed_profiles.draft.steps }}` → Steps for the 'draft' profile

**`runtime`** — `runtime.settings.<allowlisted_key>`

A pre-resolved snapshot of allowlisted settings (resolved once per build with the authenticated user), NOT a live settings-manager call. The main allowlisted key is file_storage_directory.

- `{{ runtime.settings.file_storage_directory }}` → The configured storage directory

**`paths`** — `paths.preset`

Filesystem anchors known at build time. `paths.preset` is the preset's root directory (used by external `children:` tab references).

- `"{{ paths.preset }}/modes/txt2img/tabs/advanced.yml"` → Resolved tab-fragment path

**Present, unused by any shipped preset today** (a separate card decides whether to remove them - kept documented rather than silently dropped): `preset.speed_profiles`, `preset.configuration`, `generation.seed`, `generation.quantity`, `request.mode`, `request.form_name`, `path()`, `icon()`, `matches (filter)`.

## `@config:<key>` indirection

Not a Jinja construct: a bare string-prefix match (`str.startswith('@config:')`) resolved in `src/features/presets/configuration.py`'s `resolve_filter_tags`/`resolve_field_filter_tags` at form-schema time, against the preset's admin-set `configuration:` values (see "Configuration (admin-set)" in the Preset Authoring Guide). A key with no `@config:` prefix, or a value the preset's stored configuration doesn't have, both resolve to "no filtering" - a typo in the key falls through silently rather than erroring.

## Injected form keys

Runtime-injected documents that arrive as ordinary keys of the `form` context root without being declared as form fields (`src/features/presets/linter.py`'s `_INJECTED_FORM_KEYS` - the preset linter never flags a `{{ form.<name> }}` reference to one of these as an undeclared field):

`llm`, `music_director`, `prompt_timeline`, `timeline`, `video_director`

## `model_type` values

The 18 model types a `model`/`lora_picker` field's `configuration.model_type` may select, and the depot directory each maps to (`src/platform/filesystem/model_types.py`):

| `model_type` | Directory |
|---|---|
| `adetailer` | `adetailer/` |
| `checkpoint` | `checkpoints/` |
| `controlnet` | `controlnet/` |
| `detection_bbox` | `detection_bbox/` |
| `detection_segm` | `detection_segm/` |
| `diffusion_model` | `diffusion_models/` |
| `embedding` | `embeddings/` |
| `facerestore` | `facerestore/` |
| `insightface` | `insightface/` |
| `instantid` | `instantid/` |
| `llm` | `llm/` |
| `lora` | `loras/` |
| `mediapipe` | `mediapipe/` |
| `text_encoder` | `text_encoders/` |
| `unet` | `unet/` |
| `upscaler` | `upscalers/` |
| `vae` | `vae/` |
| `vfi` | `vfi/` |

## Built-in field types

The 34 core field type names registered by `src/features/fields/builtin.py` (the live, authoritative list is served at `GET /api/fields/types`; a plugin can add more via its manifest's `field_types:` section):

`accordion`, `alert`, `audio`, `boolean`, `camera_shot`, `carousel`, `checkbox`, `checkbox_group`, `file`, `gate`, `group`, `header`, `image`, `integer`, `llm`, `lora_picker`, `markdown`, `media`, `model`, `models`, `number`, `prompt_timeline`, `resolution`, `row`, `section`, `seed`, `select`, `slider`, `stepper`, `string`, `tab`, `tabs`, `textbox`, `video`

## `content/presets/_shared/**` option files

Shared option-file vocabulary referenced from any preset via `{{ paths._shared }}` (see "External option files" in the Preset Authoring Guide). First three entries of each file:

| File | First entries |
|---|---|
| `content/presets/_shared/comfyui/form/samplers/all.yml` | {'label': 'Euler', 'value': 'euler'}; {'label': 'Euler Ancestral', 'value': 'euler_ancestral'}; {'label': 'Euler CFG++', 'value': 'euler_cfg_pp'} |
| `content/presets/_shared/comfyui/form/samplers/base.yml` | {'label': 'Euler', 'value': 'euler'}; {'label': 'Euler Ancestral', 'value': 'euler_ancestral'}; {'label': 'Euler CFG++', 'value': 'euler_cfg_pp'} |
| `content/presets/_shared/comfyui/form/samplers/extended.yml` | {'label': 'DPM++ 3SDE', 'value': 'dpmpp_3sde'}; {'label': 'RES MULTISTEP', 'value': 'res_multistep'}; {'label': 'RES MULTISTEP A', 'value': 'res_multistep_ancestral'} |
| `content/presets/_shared/comfyui/form/schedulers/all.yml` | {'label': 'Normal', 'value': 'normal'}; {'label': 'Simple', 'value': 'simple'}; {'label': 'Karras', 'value': 'karras'} |
| `content/presets/_shared/comfyui/form/schedulers/base.yml` | {'label': 'Normal', 'value': 'normal'}; {'label': 'Simple', 'value': 'simple'}; {'label': 'Karras', 'value': 'karras'} |
| `content/presets/_shared/comfyui/form/schedulers/extended.yml` | {'label': 'Bong Tangent', 'value': 'bong_tangent'} |
| `content/presets/_shared/resolutions/1k.yml` | {'value': '1024x1024', 'ratio': [1, 1], 'description': 'Square 1K'}; {'value': '1280x832', 'ratio': [3, 2], 'description': 'Wide Landscape 1K'}; {'value': '832x1280', 'ratio': [2, 3], 'description': 'Tall Portrait 1K'} |
| `content/presets/_shared/resolutions/2k.yml` | {'value': '2048x2048', 'ratio': [1, 1], 'description': 'Square 2K'}; {'value': '2496x1664', 'ratio': [3, 2], 'description': 'Wide Landscape 2K'}; {'value': '1664x2496', 'ratio': [2, 3], 'description': 'Tall Portrait 2K'} |
| `content/presets/_shared/resolutions/4k.yml` | {'value': '2880x2880', 'ratio': [1, 1], 'description': 'Square 4K'}; {'value': '3520x2368', 'ratio': [3, 2], 'description': 'Wide Landscape 4K'}; {'value': '2368x3520', 'ratio': [2, 3], 'description': 'Tall Portrait 4K'} |
| `content/presets/_shared/resolutions/flux.yml` | {'value': '1024x1024', 'ratio': [1, 1], 'description': 'Square', 'tier': 'Full'}; {'value': '1140x1472', 'ratio': [5, 6], 'description': 'Portrait', 'tier': 'Full'}; {'value': '1472x1140', 'ratio': [6, 5], 'description': 'Landscape', 'tier': 'Full'} |
| `content/presets/_shared/resolutions/flux_2k.yml` | {'value': '2048x2048', 'ratio': [1, 1], 'description': 'Square 2K'}; {'value': '2560x1440', 'ratio': [16, 9], 'description': 'Widescreen 2K'}; {'value': '1440x2560', 'ratio': [9, 16], 'description': 'Vertical 2K'} |
| `content/presets/_shared/resolutions/sdxl.yml` | {'value': '1024x1024', 'ratio': [1, 1], 'description': 'Square', 'tier': 'Full'}; {'value': '1152x896', 'ratio': [9, 7], 'description': 'Landscape', 'tier': 'Full'}; {'value': '896x1152', 'ratio': [7, 9], 'description': 'Portrait', 'tier': 'Full'} |
| `content/presets/_shared/resolutions/social_media.yml` | {'value': '1920x1080', 'ratio': [16, 9], 'description': 'YouTube / Twitter'}; {'value': '1080x1080', 'ratio': [1, 1], 'description': 'Instagram Square'}; {'value': '1080x1350', 'ratio': [4, 5], 'description': 'Instagram Portrait'} |
| `content/presets/_shared/resolutions/social_media_video.yml` | {'value': '720x720', 'ratio': [1, 1], 'description': 'Square'}; {'value': '960x960', 'ratio': [1, 1], 'description': 'Square (720P)'}; {'value': '576x720', 'ratio': [4, 5], 'description': 'Portrait (4:5)'} |
| `content/presets/_shared/resolutions/video.yml` | {'value': '832x480', 'ratio': [16, 9], 'description': 'SD Landscape'}; {'value': '480x832', 'ratio': [9, 16], 'description': 'SD Portrait'}; {'value': '960x544', 'ratio': [16, 9], 'description': 'HQ Landscape'} |
| `content/presets/_shared/resolutions/zimage.yml` | {'value': '512x512', 'ratio': [1, 1], 'description': 'Minimum Square', 'tier': 'Minimum'}; {'value': '576x448', 'ratio': [9, 7], 'description': 'Minimum Landscape', 'tier': 'Minimum'}; {'value': '448x576', 'ratio': [7, 9], 'description': 'Minimum Portrait', 'tier': 'Minimum'} |
