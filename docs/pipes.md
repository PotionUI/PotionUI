---
title: Pipes Reference
category: Presets / Models
category_order: 70
order: 12
---

# Pipes Reference

Generated by `scripts/pipes_reference.py` from the live pipe catalog (`src/pipelines/catalog.py`) — every pipe under `src/pipelines/pipes`, its `name:` key (as written in a `pipeline.yml` step), description, inputs, outputs, and configuration. Regenerate with `python scripts/pipes_reference.py`; `--check` (wired into `scripts/docs_lint.py`) fails the build when this file drifts from the code. See the [Preset Authoring Guide](presets.md) for how these are wired into a `pipeline.yml`, and Help → Documentation → Pipes in the running app for the same data against that install's actually enabled plugins.

## Families

| Family | Pipes |
|---|---|
| `adm_guidance` | [`adm_guidance/sdxl`](#pipe-adm-guidance-sdxl) |
| `artifact` | [`artifact`](#pipe-artifact) |
| `audio_trim` | [`audio_trim`](#pipe-audio-trim) |
| `canvas_fit` | [`canvas_fit`](#pipe-canvas-fit) |
| `checkpoint_loader` | [`checkpoint_loader/sdxl`](#pipe-checkpoint-loader-sdxl) |
| `color_key` | [`color_key`](#pipe-color-key) |
| `controlnet_loader` | [`controlnet_loader/sdxl`](#pipe-controlnet-loader-sdxl) |
| `controlnet_preprocessor` | [`controlnet_preprocessor`](#pipe-controlnet-preprocessor) |
| `crop_subject` | [`crop_subject`](#pipe-crop-subject) |
| `detailer` | [`detailer/sdxl`](#pipe-detailer-sdxl), [`detailer/video_ltx`](#pipe-detailer-video-ltx) |
| `dynamic_prompts_renderer` | [`dynamic_prompts_renderer`](#pipe-dynamic-prompts-renderer) |
| `film_grain` | [`film_grain`](#pipe-film-grain) |
| `from_iotype` | [`from_iotype`](#pipe-from-iotype) |
| `gallery` | [`gallery`](#pipe-gallery) |
| `generator` | [`generator/anima`](#pipe-generator-anima), [`generator/audio_minimax_music3`](#pipe-generator-audio-minimax-music3), [`generator/chain_video_wan22`](#pipe-generator-chain-video-wan22), [`generator/dfr_video_ltx`](#pipe-generator-dfr-video-ltx), [`generator/flux`](#pipe-generator-flux), [`generator/img2vid_wan22`](#pipe-generator-img2vid-wan22), [`generator/krea2`](#pipe-generator-krea2), [`generator/maya`](#pipe-generator-maya), [`generator/qwen`](#pipe-generator-qwen), [`generator/sdxl`](#pipe-generator-sdxl), [`generator/seedvr2`](#pipe-generator-seedvr2), [`generator/trellis2`](#pipe-generator-trellis2), [`generator/txt2vid_ltx`](#pipe-generator-txt2vid-ltx), [`generator/txt2vid_wan22`](#pipe-generator-txt2vid-wan22), [`generator/video_ltx`](#pipe-generator-video-ltx), [`generator/video_minimax_h3`](#pipe-generator-video-minimax-h3), [`generator/z_image`](#pipe-generator-z-image) |
| `inpaint_region_crop` | [`inpaint_region_crop`](#pipe-inpaint-region-crop) |
| `inpaint_region_restore` | [`inpaint_region_restore`](#pipe-inpaint-region-restore) |
| `interpolator` | [`interpolator/rife`](#pipe-interpolator-rife) |
| `latent_upscaler` | [`latent_upscaler/ltx`](#pipe-latent-upscaler-ltx) |
| `mask_preprocessor` | [`mask_preprocessor`](#pipe-mask-preprocessor) |
| `matting` | [`matting/birefnet`](#pipe-matting-birefnet) |
| `media_loader` | [`media_loader`](#pipe-media-loader) |
| `model_loader` | [`model_loader/anima`](#pipe-model-loader-anima), [`model_loader/flux`](#pipe-model-loader-flux), [`model_loader/krea2`](#pipe-model-loader-krea2), [`model_loader/ltx`](#pipe-model-loader-ltx), [`model_loader/maya`](#pipe-model-loader-maya), [`model_loader/minimax_h3`](#pipe-model-loader-minimax-h3), [`model_loader/minimax_music3`](#pipe-model-loader-minimax-music3), [`model_loader/qwen`](#pipe-model-loader-qwen), [`model_loader/seedvr2`](#pipe-model-loader-seedvr2), [`model_loader/trellis2`](#pipe-model-loader-trellis2), [`model_loader/wan22`](#pipe-model-loader-wan22), [`model_loader/z_image`](#pipe-model-loader-z-image) |
| `output_skipper` | [`output_skipper`](#pipe-output-skipper) |
| `param_emitter` | [`param_emitter`](#pipe-param-emitter) |
| `prompt_encoder` | [`prompt_encoder`](#pipe-prompt-encoder) |
| `prompt_expander` | [`prompt_expander`](#pipe-prompt-expander) |
| `sag` | [`sag/sdxl`](#pipe-sag-sdxl) |
| `seed_generator` | [`seed_generator`](#pipe-seed-generator) |
| `sharpness` | [`sharpness/sdxl`](#pipe-sharpness-sdxl) |
| `tiled_detailer` | [`tiled_detailer/sdxl`](#pipe-tiled-detailer-sdxl) |
| `tiled_refiner` | [`tiled_refiner`](#pipe-tiled-refiner) |
| `upscaler` | [`upscaler`](#pipe-upscaler) |
| `video_frame_extractor` | [`video_frame_extractor`](#pipe-video-frame-extractor) |
| `video_frame_merger` | [`video_frame_merger`](#pipe-video-frame-merger) |
| `video_speed` | [`video_speed`](#pipe-video-speed) |

## adm_guidance

### <a id="pipe-adm-guidance-sdxl"></a>`adm_guidance/sdxl`

ADM Guidance enhancement for SDXL (Fooocus technique)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | SDXL model with hook registry |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `model` | `MODEL` | no | Model with ADM hook registered |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `positive_scale` | `float` | `1.5` | no | — | 1.0 | 2.0 | ADM positive conditioning scale |
| `negative_scale` | `float` | `0.8` | no | — | 0.5 | 1.0 | ADM negative conditioning scale |
| `scaler_end` | `float` | `0.3` | no | — | 0.0 | 1.0 | Progress threshold to stop ADM |

## artifact

### <a id="pipe-artifact"></a>`artifact`

This pipe will generate artifacts

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `before_image` | `IMAGE` | yes | yes | Images for comparison or processing |
| `after_image` | `IMAGE` | yes | yes | Images for comparison or processing |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Processed or selected images |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `mode` | `str` | `"compare"` | yes | `compare` | — | — | Artifact processing mode |
| `left` | `str` | `"original_image"` | yes | — | — | — | Left side image identifier for comparison |
| `right` | `str` | `"final_image"` | yes | — | — | — | Right side image identifier for comparison |
| `output` | `str` | `"right"` | yes | `left`, `right` | — | — | Which image to output |

## audio_trim

### <a id="pipe-audio-trim"></a>`audio_trim`

Trim audio to a start/duration window, sample-accurate, without decoding the whole file

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `audio` | `AUDIO` | yes | yes | Source audio file path(s) to trim |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `audio` | `AUDIO` | yes | Trimmed audio file path(s) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `start_seconds` | `float` | `0.0` | no | — | 0.0 | — | Where the kept window starts, in seconds from the source's beginning |
| `duration_seconds` | `float` | `10.0` | no | — | 0.0 | — | How many seconds to keep from start_seconds - clamped to the source's remaining length |

## canvas_fit

### <a id="pipe-canvas-fit"></a>`canvas_fit`

Scale an image (aspect-preserved) and place it on a fixed canvas

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `image` | `IMAGE` | yes | yes | Source image(s) to scale and place |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Fixed-size canvas(es) with the scaled image placed on it |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `width` | `int` | `1024` | yes | — | 1 | — | Canvas width (px) |
| `height` | `int` | `1024` | yes | — | 1 | — | Canvas height (px) |
| `scale_percent` | `float` | `100.0` | no | — | 0.0 | — | Percentage of the CANVAS's short side that the image's long side should occupy |
| `anchor` | `str` | `"center"` | no | `center`, `top`, `bottom`, `left`, `right`, `top_left`, `top_right`, `bottom_left`, `bottom_right` | — | — | Where to place the scaled image on the canvas |
| `fill` | `str` | `"transparent"` | no | — | — | — | 'transparent' (keep alpha) or a hex colour (opaque RGB output) |

## checkpoint_loader

### <a id="pipe-checkpoint-loader-sdxl"></a>`checkpoint_loader/sdxl`

Specialized pipe for loading SDXL models with optimized settings

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `GPU` | `SERVICE` | no | no | GPU manager service for VRAM monitoring |
| `MEMORY` | `SERVICE` | no | no | Memory manager service for intelligent allocation |
| `MODELS` | `SERVICE` | no | no | Model lifecycle service for cross-generation model reuse |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `model` | `MODEL` | no | Loaded SDXL model |
| `text_encoder` | `TEXT_ENCODER` | no | CLIP text encoder for prompt processing |
| `vae` | `VAE` | no | VAE for image encoding/decoding |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `model` | `dict` | — | yes | — | — | — | Model template configuration |
| `loras` | `list` | `[]` | no | — | — | — | List of LoRA configurations |
| `mode` | `str` | `"txt2img"` | no | `txt2img`, `img2img` | — | — | Generation mode (txt2img, img2img, etc.) |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu`, `mps` | — | — | Device to load model on |
| `dtype` | `str` | `"float16"` | no | `float16`, `float32`, `bfloat16` | — | — | Data type for model weights |
| `nsfw` | `bool` | `false` | no | — | — | — | Enable NSFW content filtering |
| `clip_skip` | `int` | — | no | — | 0 | 12 | Number of CLIP layers to skip (None for default SDXL behavior) |
| `vram_limit_gb` | `float` | — | no | — | 4.0 | 128.0 | GPU VRAM limit in GB (None = auto-detect based on hardware) |
| `extras` | `dict` | `{}` | no | — | — | — | Extra configuration parameters |

## color_key

### <a id="pipe-color-key"></a>`color_key`

Chroma-key a flat background colour to alpha, keying on chrominance (brightness-invariant)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `image` | `IMAGE` | yes | yes | Source image(s) with a flat/near-flat background |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | RGBA image(s) with the background keyed to alpha |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `key_mode` | `str` | `"auto"` | no | `auto`, `color` | — | — | 'auto' (samples the dominant border colour) or 'color' (explicit key_color) |
| `key_color` | `str` | `"#00B140"` | no | — | — | — | Hex colour to key out (key_mode='color' only) |
| `tolerance` | `float` | `25.0` | no | — | 0.0 | 100.0 | Chrominance distance from the key colour that counts as background (0-100) |
| `softness` | `float` | `10.0` | no | — | 0.0 | 100.0 | Width of the soft alpha ramp around the tolerance boundary (0-100) |
| `despill` | `bool` | `true` | no | — | — | — | Suppress the key colour's dominant channel on every pixel |
| `feather` | `float` | `0.0` | no | — | 0.0 | 16.0 | Gaussian-blur radius (px) applied to the alpha edge |

## controlnet_loader

### <a id="pipe-controlnet-loader-sdxl"></a>`controlnet_loader/sdxl`

Load SDXL ControlNet models for guided generation

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `MODELS` | `SERVICE` | no | no | Model lifecycle service for cross-generation reuse |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `controlnet` | `CONTROLNET` | yes | Loaded ControlNet model(s) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `controlnets` | `list` | `[]` | no | — | — | — | List of ControlNet configurations |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu`, `mps` | — | — | Device to load ControlNets on |
| `dtype` | `str` | `"float16"` | no | `float16`, `float32`, `bfloat16` | — | — | Data type for ControlNet weights |

## controlnet_preprocessor

### <a id="pipe-controlnet-preprocessor"></a>`controlnet_preprocessor`

Preprocess images for ControlNet (Canny, Depth, OpenPose, etc.)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `image` | `IMAGE` | yes | yes | Input images to preprocess |
| `ASSETS` | `SERVICE` | no | no | Asset fetcher, to mirror the annotator weights into the depot |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Preprocessed control images |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `preprocessors` | `list` | `[]` | no | — | — | — | List of preprocessor configurations |
| `output_resolution` | `list` | — | no | — | — | — | Optional output resolution [width, height] |

## crop_subject

### <a id="pipe-crop-subject"></a>`crop_subject`

Crop to the alpha bounding box (plus margin); passes through unchanged when an image has no opaque subject

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `image` | `IMAGE` | yes | yes | Source image(s) (an RGBA cutout is expected, but any image works) |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Cropped image(s), or the input unchanged where no subject was found |
| `cropped` | `BOOL` | yes | Whether a crop was actually applied, per image |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `margin` | `int` | `0` | no | — | 0 | — | Padding (px) added around the alpha bounding box on every side |
| `alpha_threshold` | `int` | `16` | no | — | 0 | 255 | Alpha value (0-255) a pixel must exceed to count toward the subject bbox |

## detailer

### <a id="pipe-detailer-sdxl"></a>`detailer/sdxl`

SDXL-optimized automatic detection and enhancement of faces and hands

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `conditioning` | `CONDITIONING` | yes | yes | Encoded prompt conditioning |
| `model` | `MODEL` | yes | no | AI model for image generation |
| `seed` | `SEED` | no | yes | Random seeds for generation |
| `image` | `IMAGE` | yes | yes | Input images to enhance |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Enhanced images with detailed faces and hands |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `detect` | `list` | `["face", "hand"]` | no | `face`, `hand`, `eyes`, `teeth`, `person` | — | — | Types of objects to detect |
| `detections` | `dict` | `{}` | no | — | — | — | Detection configuration for each object type |
| `confidence` | `float` | `0.35` | no | — | 0.0 | 1.0 | Detection confidence threshold |
| `mask_min_ratio` | `float` | `0.008` | no | — | 0.001 | 1.0 | Minimum mask size ratio |
| `mask_max_ratio` | `float` | `0.6` | no | — | 0.1 | 1.0 | Maximum mask size ratio |
| `strength` | `float` | `0.12` | no | — | 0.0 | 0.5 | Denoising strength (SDXL optimized) |
| `cfg` | `float` | `5.5` | no | — | 1.0 | 15.0 | CFG scale (SDXL optimized) |
| `steps` | `int` | `25` | no | — | 10 | 50 | Number of inference steps (SDXL optimized) |
| `padding` | `int` | `32` | no | — | 0 | 200 | Padding around detected regions |
| `mask_blur` | `int` | `0` | no | — | 0 | 50 | Mask blur radius (SDXL optimized) |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu`, `mps` | — | — | Device to use for processing |
| `box_color` | `list` | `[0, 0, 0]` | no | — | — | — | Detection box color (RGB) |
| `box_thickness` | `int` | `8` | no | — | 1 | 50 | Detection box thickness |
| `size_threshold` | `float` | `0.08` | no | — | 0.01 | 1.0 | Minimum detection size threshold |
| `step_reduction_percentage` | `float` | `0.6` | no | — | 0.1 | 1.0 | Step reduction for large images |
| `enable_upscale` | `bool` | `true` | no | — | — | — | Enable upscaling before detailing |
| `upscale_factor` | `float` | `1.5` | no | — | 1.0 | 2.5 | Conservative upscaling factor for SDXL |
| `upscale_method` | `str` | `"LANCZOS"` | no | `LANCZOS`, `NEAREST`, `BILINEAR`, `BICUBIC` | — | — | Upscaling interpolation method |
| `min_size_for_upscale` | `int` | `96` | no | — | 32 | 512 | Minimum face size to apply upscaling |
| `sampler` | `str` | `"DPMPP_2M"` | no | `EULER`, `EULER_A`, `HEUN`, `DPM2`, `DPM2_A`, `LMS`, `DPMPP_2S_A`, `DPMPP_SDE`, `DPMPP_2M`, `DPMPP_2M_SDE`, `DPMPP_3M_SDE`, `LCM` | — | — | Sampling algorithm to use |
| `scheduler` | `str` | `"karras"` | no | `normal`, `karras`, `exponential`, `sgm_uniform`, `simple`, `ddim_uniform` | — | — | Noise schedule type to use |

### <a id="pipe-detailer-video-ltx"></a>`detailer/video_ltx`

Refine faces and hands in an LTX-2/2.3 video as stabilized spatiotemporal tubes (same model, light denoise, feathered paste-back)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | LTX model bundle (same model that generated the clip) |
| `video` | `VIDEO` | yes | yes | Video file(s) to refine |
| `conditioning` | `CONDITIONING` | yes | yes | Encoded prompt conditioning (the generation's positive prompt) |
| `seed` | `SEED` | no | yes | Random seeds (per video, for the refine noise) |
| `MODELS` | `SERVICE` | no | no | Model lifecycle service |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `video` | `VIDEO` | yes | Refined video file(s) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `strength` | `str` | `"balanced"` | no | `light`, `balanced`, `strong` | — | — | Refinement strength (starting sigma) |
| `detect_faces` | `bool` | `true` | no | — | — | — | Detect and refine faces |
| `detect_hands` | `bool` | `true` | no | — | — | — | Detect and refine hands |
| `detection_backend` | `str` | `"mediapipe"` | no | `mediapipe`, `yolo` | — | — | Detector backend (mediapipe = Apache-2.0 default; yolo = AGPL, opt-in) |
| `detection_stride` | `int` | `6` | no | — | 1 | 60 | Detect every Nth frame |
| `detection_confidence` | `float` | `0.5` | no | — | 0.0 | 1.0 | Detector confidence threshold |
| `max_tracks` | `int` | `4` | no | — | 1 | 16 | Max subjects to refine per clip |
| `min_track_seconds` | `float` | `0.5` | no | — | 0.0 | 10.0 | Discard tracks shorter than this |
| `iou_threshold` | `float` | `0.3` | no | — | 0.0 | 1.0 | IoU threshold for linking detections into tracks |
| `cut_threshold` | `float` | `0.35` | no | — | 0.0 | 1.0 | Histogram distance above which a scene cut splits a track |
| `pad_factor` | `float` | `1.8` | no | — | 1.0 | 4.0 | Context padding around each subject's window |
| `area_threshold` | `float` | `0.4` | no | — | 0.05 | 1.0 | Fraction of frame area above which the tube window follows the subject instead of holding fixed |
| `working_short_side` | `int` | `512` | no | — | 128 | 1024 | Refine working-resolution short side (px) |
| `color_correction` | `str` | `"wavelet"` | no | `wavelet`, `adain`, `none` | — | — | Match refined patch colour to the original |
| `feather_border_frac` | `float` | `0.08` | no | — | 0.0 | 0.5 | Spatial feather border (fraction of tube size) |
| `temporal_ramp_frames` | `int` | `4` | no | — | 0 | 30 | Fade-in/out length at each tube segment end (frames) |
| `face_model` | `str` | `"models/mediapipe/face_landmarker.task"` | no | — | — | — | MediaPipe face landmarker model path |
| `hand_model` | `str` | `"models/mediapipe/hand_landmarker.task"` | no | — | — | — | MediaPipe hand landmarker model path |

## dynamic_prompts_renderer

### <a id="pipe-dynamic-prompts-renderer"></a>`dynamic_prompts_renderer`

Emits the fully-rendered per-image prompts ({a|b}, ${variables} and phrasebook already resolved on the backend) as provenance artifacts

_No inputs._

**Outputs**: none.

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `pairs` | `list` | `[]` | no | — | — | — | Per-image expanded prompt pairs (from generation.prompts.pairs) |
| `quantity` | `int` | `1` | no | — | 1 | 20 | Number of images being generated (fills the batch when only pairs[0] was produced) |

## film_grain

### <a id="pipe-film-grain"></a>`film_grain`

Adds realistic film grain effect to make images look more natural

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `image` | `IMAGE` | yes | yes | Input images to apply film grain effect |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Images with film grain effect applied |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `intensity` | `float` | `0.3` | no | — | 0.0 | 1.0 | Controls the overall strength of the grain effect |
| `grain_size` | `float` | `1.5` | no | — | 0.5 | 3.0 | Controls the size of grain particles |
| `shadows_bias` | `float` | `0.6` | no | — | 0.0 | 1.0 | More grain in darker areas |
| `highlights_protect` | `float` | `0.2` | no | — | 0.0 | 1.0 | Protect highlights from grain |
| `monochromatic` | `bool` | `false` | no | — | — | — | If True, applies same grain to all channels |
| `blur_amount` | `float` | `0.5` | no | — | 0.0 | 2.0 | Slight blur to make grain look more natural |
| `preset` | `str` | — | no | `classic_35mm`, `cinematic_portra`, `vintage_push`, `modern_cine`, `high_iso`, `super8`, `subtle_digital`, `trix400` | — | — | Preset film grain style |

## from_iotype

### <a id="pipe-from-iotype"></a>`from_iotype`

Converts IOType values to regular output values for use in other pipes

_No inputs._

**Outputs**: none.

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `from` | `str` | — | no | — | — | — | Single input to convert (simple mode) |
| `mappings` | `list` | `[]` | no | — | — | — | List of input->output mappings |

## gallery

### <a id="pipe-gallery"></a>`gallery`

Pipe that will output an gallery

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `image` | `IMAGE` | no | yes | List of images to output |
| `video` | `VIDEO` | no | yes | List of videos to output |
| `audio` | `AUDIO` | no | yes | List of audio files to output |
| `seed` | `SEED` | no | yes | Seed used for generation |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | List of images that were inputted (it just passes them forward) |
| `video` | `VIDEO` | yes | List of videos that were inputted (it just passes them forward) |
| `audio` | `AUDIO` | yes | List of audio files that were inputted (it just passes them forward) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `derived` | `bool` | `false` | no | — | — | — | Mark emitted media as derived from another final output of this generation (e.g. an enhance pass). Presentation hint only - persisted file order is unchanged. |

## generator

### <a id="pipe-generator-anima"></a>`generator/anima`

Native Anima generator (flow matching, true CFG, LLMAdapter text fusion)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | Anima model bundle |
| `conditioning` | `CONDITIONING` | yes | yes | Encoded prompt conditioning (per image) |
| `seed` | `SEED` | no | yes | Random seeds |
| `image` | `IMAGE` | no | yes | Input images for img2img mode |
| `MODELS` | `SERVICE` | no | no | Model lifecycle service, to release the idle TE's VRAM before sampling |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Generated images |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `mode` | `str` | `"txt2img"` | yes | `txt2img`, `img2img` | — | — | Generation mode |
| `denoise` | `float` | `0.55` | no | — | 0.0 | 1.0 | img2img denoise strength (fraction of the schedule to walk) |
| `img2img_scale` | `float` | `0.0` | no | — | 0.0 | 8.0 | Literal multiplier of the SOURCE image's own (width, height) for the img2img target size, snapped to the model's granularity same as every other path here. 0 (default) is off and leaves the existing resolution-area-match / area-cap behaviour byte-identical -- set this instead of (not alongside) the mode's ``resolution`` config for a true "upscale by Nx of whatever was uploaded" mode. |
| `steps` | `int` | `24` | no | — | 1 | 100 | Denoising steps |
| `guidance` | `float` | `6.0` | no | — | 0.0 | 30.0 | True CFG scale |
| `shift` | `float` | — | no | — | — | — | Sigma-shift override; blank -> spec default (3.0) |
| `sampler` | `str` | `"euler"` | no | `euler`, `dpmpp_2m`, `dpmpp_2m_sde`, `dpmpp_3m`, `res_multistep`, `unipc`, `lcm` | — | — | Sampler |
| `resolution` | `str` | `"1024x1024"` | no | — | — | — | Resolution (WxH) |
| `quantity` | `int` | `1` | no | — | 1 | 10 | Number of images |
| `seed` | `int` | `-1` | no | — | -1 | — | Random seed |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `preview` | `bool` | `true` | no | — | — | — | Emit live latent previews to the workbench during sampling |
| `step_cache` | `dict` | `{}` | no | — | — | — | FBCache step-skipping options, forwarded to NativeGenerator.sample() unmodified: {'rel_threshold': 0.12, 'warmup_steps': 4, 'max_consecutive_skips': 3}. rel_threshold<=0 (default/absent) is off and never wraps the guidance strategy -- byte-identical to leaving this unset. Read directly by FlowMatchGeneratorPipe.build_context, not through the flat step_cache_threshold/warmup_steps/max_skips resolver the Wan/LTX video pipes use (this family has no such resolver). |

### <a id="pipe-generator-audio-minimax-music3"></a>`generator/audio_minimax_music3`

Native MiniMax-Music3 text-to-music generator

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | MiniMax-Music3 model bundle |
| `MODELS` | `SERVICE` | no | no | Model lifecycle service, to release the AR core before the DiT places |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `audio` | `AUDIO` | yes | Generated song(s), one per seed |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `caption` | `str` | `""` | yes | — | — | — | The song's caption -- genre/mood/instrumentation/vocals, composed by the preset from its form fields |
| `lyrics` | `str` | `""` | no | — | — | — | Lyrics with [tag] structure markers, one per line (e.g. '[instrumental]' for no vocals) |
| `duration` | `float` | `60.0` | no | — | 0.0 | 360.0 | Upper bound on song length in seconds; the AR stage may stop earlier and report the actual length. 0 = auto (run to the model's own stop token). Hard cap 360s |
| `steps` | `int` | `30` | no | — | 1 | 100 | Flow-matching Euler steps per denoising window |
| `seed` | `int` | `-1` | no | — | -1 | — | Random seed (drives both the AR stage and every flow window) |
| `ar_cfg_scale` | `float` | `1.5` | no | — | 1.0 | 10.0 | Classifier-free guidance for the AR (semantic + residual code) sampling stage |
| `cfg_scale` | `float` | `1.7` | no | — | 1.0 | 10.0 | Classifier-free guidance for the flow-matching DiT |
| `top_k` | `int` | `50` | no | — | 1 | 1024 | Top-k restriction for AR sampling |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |

### <a id="pipe-generator-chain-video-wan22"></a>`generator/chain_video_wan22`

Native Wan 2.2 sequential chain-video generator (SVI Pro 2.0-style continuation)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | no | no | i2v Wan bundle (base LoRA stacks applied); runs i2v/flf/chain segments |
| `model_t2v` | `MODEL` | no | no | t2v Wan bundle (base LoRA stacks applied); runs t2v segments |
| `conditioning` | `CONDITIONING` | yes | yes | Encoded prompt conditioning, one per chain segment (index-aligned) |
| `image` | `IMAGE` | no | yes | Start image(s), one per i2v/flf segment (document-order, role=first) |
| `end_image` | `IMAGE` | no | yes | End image(s), one per flf segment (document-order, role=last) |
| `MODELS` | `SERVICE` | no | no | Model lifecycle service, for per-segment LoRA re-acquisition |
| `GPU` | `SERVICE` | no | no | GPU manager for the VRAM budget (re-acquisition loader) |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `video` | `VIDEO` | yes | Generated videos (one per segment, plus the stitched result when enabled) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `mode` | `str` | `"chain"` | yes | `chain` | — | — | Generation mode |
| `document` | `dict` | — | yes | — | — | — | Canonical Video Director document (schema_version 1, mode='chain'); segments[] drives the sequential loop |
| `motion_latent_count` | `int` | `1` | no | — | 1 | 4 | SVI Pro: how many temporal LATENT slots of the previous segment's tail carry into the next segment's conditioning (1 slot ~= 4 pixel frames). Lower keeps a lighter motion hand-off; raise to re-inject the full tail |
| `anchor_latent_strength` | `float` | `1.0` | no | — | 0.0 | 1.0 | SVI Pro: mask weight on each segment's anchor (start) frames. 1.0 hard-locks them (identity); lower loosens the lock so dynamic scenes can move off the anchor |
| `seam_handoff` | `str` | `"latent"` | no | `latent`, `pixel` | — | — | Chain continuation seam conditioning. 'latent' (default) splices the previous segment's own SAMPLED latent tail directly into the next segment's locked slots, bypassing the decode->uint8->re-encode round trip (removes two of the three seam color-shift sources). 'pixel' reproduces the original round-tripped hand-off exactly, for A/B comparison against the previous default. |
| `steps` | `int` | `30` | no | — | 1 | 150 | Default denoising steps (segment override: segments[i].steps) |
| `cfg` | `float` | `5.0` | no | — | 0.0 | 30.0 | Default true CFG scale (segment override: segments[i].cfg) |
| `sampler` | `str` | `"unipc"` | no | `euler`, `dpmpp_2m`, `dpmpp_2m_sde`, `dpmpp_3m`, `res_multistep`, `unipc`, `lcm` | — | — | Sampler |
| `resolution` | `str` | `"832x480"` | no | — | — | — | Resolution (WxH), constant across all segments |
| `fps` | `float` | `16.0` | no | — | 1.0 | 60.0 | Output frame rate |
| `expert_boundary` | `float` | — | no | — | 0.0 | 1.0 | Dual-expert switch boundary override (sigma fraction) |
| `expert_switch_step` | `int` | — | no | — | 0 | — | Switch high->low expert at this step (wins over expert_boundary; converted to that step's sigma) |
| `dtype` | `str` | `"bfloat16"` | no | `bfloat16`, `float16`, `float32` | — | — | Compute dtype (must match model_loader/wan22's dtype for cache reuse on re-acquire) |
| `t2v_high_noise_model` | `dict` | — | no | — | — | — | t2v high-noise (or only) DiT file -- re-acquire target for a t2v segment's LoRA swap |
| `t2v_low_noise_model` | `dict` | — | no | — | — | — | t2v low-noise expert DiT file (Wan 2.2 dual-expert only) |
| `i2v_high_noise_model` | `dict` | — | no | — | — | — | i2v high-noise (or only) DiT file -- re-acquire target for an i2v/chain segment's LoRA swap |
| `i2v_low_noise_model` | `dict` | — | no | — | — | — | i2v low-noise expert DiT file (Wan 2.2 dual-expert only) |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `preview` | `bool` | `true` | no | — | — | — | Emit live latent previews to the workbench during sampling |
| `cfg_zero_star` | `bool` | `true` | no | — | — | — | CFG-Zero*: rescale the uncond branch onto cond before extrapolation |
| `zero_init_steps` | `int` | `0` | no | — | 0 | 100 | CFG-Zero*: zero velocity for the first N steps |
| `nag_scale` | `float` | `1.0` | no | — | 1.0 | 20.0 | Normalized Attention Guidance scale (1.0 = off). Injects the negative prompt into cross-attention so it's enforced even at cfg=1.0 (single-forward speed) — set cfg to 1.0 and nag_scale to ~1.1-1.5 for the speed win; NAG stacks with true CFG if both are on. |
| `nag_tau` | `float` | `3.5` | no | — | 0.1 | 20.0 | NAG norm-clamp threshold (paper default 3.5) |
| `nag_alpha` | `float` | `0.5` | no | — | 0.0 | 1.0 | NAG blend-back-toward-positive weight (paper default 0.5) |
| `riflex` | `bool` | `false` | no | — | — | — | RIFLEx: extrapolate video length beyond the model's trained frame count via intrinsic RoPE frequency clamping (default off = byte-identical schedule) |
| `riflex_trained_frames` | `int` | — | no | — | 1 | — | RIFLEx: override the trained latent-frame count (default: the family's known trained length; only used when riflex=true) |
| `apg_eta` | `float` | — | no | — | 0.0 | 1.0 | APG: down-weight of the CFG delta's component parallel to the cond prediction (unset = inherit the model's own sampling_settings, or 1.0 = off / plain CFG if neither sets it; paper explores ~0.0-0.5 to reduce oversaturation at high CFG) |
| `apg_norm_threshold` | `float` | — | no | — | 0.0 | — | APG: cap the guidance delta's norm to this radius (unset = inherit; 0 = off) |
| `apg_momentum` | `float` | — | no | — | -1.0 | 1.0 | APG: reverse-momentum coefficient applied to the guidance delta across steps (unset = inherit; 0 = off; paper uses small negative values, e.g. -0.5) |
| `slg_scale` | `float` | — | no | — | 0.0 | 10.0 | Skip-Layer Guidance: push-away strength from a degraded (blocks-skipped) prediction (unset = inherit the model's own sampling_settings, or 0 = off if neither sets it) |
| `slg_layers` | `str` | — | no | — | — | — | Skip-Layer Guidance: comma-separated transformer block indices to skip in the degraded pass (unset = inherit; empty = off even if slg_scale > 0) |
| `slg_sigma_start` | `float` | — | no | — | 0.0 | 1.0 | Skip-Layer Guidance: window upper (earlier-trajectory) sigma bound (unset = inherit) |
| `slg_sigma_end` | `float` | — | no | — | 0.0 | 1.0 | Skip-Layer Guidance: window lower (later-trajectory) sigma bound (unset = inherit) |
| `sampler_options` | `dict` | `{}` | no | — | — | — | Opaque per-sampler options dict forwarded to the chosen sampler, e.g. {'eta': 0.5} for sampler=euler_sde or {'restart_count': 2, 'restart_strength': 0.3} for sampler=euler_restart. Empty/absent = sampler defaults, no effect on euler/dpmpp_2m/unipc. |
| `step_cache` | `dict` | `{}` | no | — | — | — | FBCache step-skipping options: {'rel_threshold': 0.12, 'warmup_steps': 4, 'max_consecutive_skips': 3}. rel_threshold<=0 (default/absent) is off. Only arch forwards that honor the step_cache kwarg actually skip steps; others ignore it harmlessly. Takes priority over the flat step_cache_threshold/step_cache_warmup_steps/step_cache_max_skips knobs below when non-empty (back-compat with existing presets/callers). |
| `step_cache_threshold` | `float` | `0.0` | no | — | 0.0 | 1.0 | FBCache step-skipping: relative-error threshold below which a step's residual is reused instead of recomputed. 0.0 (default) is off; ~0.08 is a conservative starting point, ~0.15 more aggressive. Only pays off on runs of 15+ steps -- warmup_steps alone already covers shorter runs. Ignored when the 'step_cache' dict above is also set. |
| `step_cache_warmup_steps` | `int` | `4` | no | — | 0 | — | FBCache step-skipping: number of leading steps forced to fully compute (never skipped) before the cache is allowed to kick in. Ignored when the 'step_cache' dict above is also set. |
| `step_cache_max_skips` | `int` | `3` | no | — | 0 | — | FBCache step-skipping: maximum number of consecutive steps the cache may skip before a forced recompute. Ignored when the 'step_cache' dict above is also set. |
| `schedule` | `str` | `""` | no | — | — | — | Sigma schedule key: '' (default) or 'shift' for the model's own shift-based ramp, or any key registered on the schedule registry -- 'beta', 'exponential', 'linear_quadratic', 'manual', 'ltx_dynamic' in core, plus whatever a plugin's 'schedules:' manifest root adds. GET /api/sampling/catalog lists what this instance accepts. Prefer 'manual_sigmas' below over setting this to 'manual' directly -- it also fills schedule_options for you. 'ltx_dynamic' only functions on a pipe that feeds image_seq_len into the schedule builder -- currently the LTX generator pipes. |
| `schedule_options` | `dict` | `{}` | no | — | — | — | Schedule-specific options: beta {'alpha': 0.6, 'beta': 0.6}, exponential {'sigma_min': 1e-3}, linear_quadratic {'threshold_noise': 0.025, 'linear_steps': <int, default steps // 2>}, or ltx_dynamic {'base_shift': 0.95, 'max_shift': 2.05, 'stretch': True, 'terminal': 0.1} (LTX-2.5 defaults). Ignored when schedule is unset. |
| `manual_sigmas` | `str` | `""` | no | — | — | — | Explicit, comma-separated, descending sigma schedule ('1.0, 0.99375, 0.9875, ..., 0.0'; ComfyUI 'ManualSigmas'-style, e.g. a distilled-LoRA refine tail -- see docs/models/ltx.md). Its length IS the step count: overrides 'steps' and any shift/schedule setting outright. Takes priority over 'schedule'/'schedule_options' when non-empty (the default '' is a no-op, byte-identical to not having this knob). |
| `detail_strength` | `float` | — | no | — | -0.3 | 0.3 | Detail-daemon sigma warp strength (unset = inherit the model's own sampling_settings, or 0 = off if neither sets it; expected range -0.3 to 0.3) |
| `detail_start` | `float` | — | no | — | 0.0 | 1.0 | Detail-daemon warp window start (trajectory fraction; unset = inherit) |
| `detail_end` | `float` | — | no | — | 0.0 | 1.0 | Detail-daemon warp window end (trajectory fraction; unset = inherit) |

### <a id="pipe-generator-dfr-video-ltx"></a>`generator/dfr_video_ltx`

Native LTX-2.5 DFR temporal rounds: densify a finished video latent to 2x/4x the frame rate with tiled, keyframe-anchored re-denoising

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | LTX model bundle -- the SAME bundle the base/refine stages used, so the rounds share their LoRA chain. Needs 'temporal_upscale_model' loaded whenever rounds > 0 |
| `conditioning` | `CONDITIONING` | yes | yes | Encoded prompt conditioning (the same the earlier stages consumed) |
| `latent` | `LATENT` | yes | yes | Finished video latent to densify -- the upscale/refine stage's raw latent output when upscaling, otherwise the base pass's |
| `seed` | `SEED` | no | yes | Random seeds |
| `image` | `IMAGE` | no | yes | Conditioning images, re-applied per tile at tile-local indices |
| `audio` | `AUDIO` | no | yes | Finished audio track from the base pass, muxed verbatim into the final encode (rounds generate no audio of their own) |
| `MODELS` | `SERVICE` | no | no | Model lifecycle service, to release the idle TE's host RAM |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `video` | `VIDEO` | yes | Densified videos (empty when decode=false) |
| `latent` | `LATENT` | yes | Densified per-seed latents (only populated when decode=false) |
| `audio` | `AUDIO` | yes | The passed-through audio track(s) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `rounds` | `int` | `1` | no | — | 0 | 2 | Temporal densification rounds. Each doubles the frame count and the playback frame rate (0 = decode the incoming latent unchanged, useful for an A/B against the same seed) |
| `fps` | `float` | `25.0` | no | — | 1.0 | 60.0 | BASE frame rate of the incoming latent. The output plays at fps * 2**rounds; the RoPE conditioning frame rate is capped at 60 |
| `frames` | `int` | `121` | no | — | 9 | 1001 | Requested pixel frames (cross-check only -- the real count is derived from the incoming latent, as in generator/video_ltx) |
| `resolution` | `str` | `"768x512"` | no | — | — | — | Expected resolution (WxH) of the incoming latent, cross-checked before any GPU work |
| `round_sigmas` | `str` | `"0.975,0.909375,0.725,0.421875,0.0"` | no | — | — | — | Sigma schedule for every tile of every round, comma-separated, descending, used VERBATIM (like generator/video_ltx's refine_sigmas: the head is NOT forced to 1.0, which is what keeps the partial-noise start intact) |
| `anchor_strength` | `float` | `0.95` | no | — | 0.0 | 1.0 | Conditioning strength of a carried seam keyframe (1 = fully pinned, 0 = free). Deliberately just short of 1.0 so the tiles either side of a seam can settle it between them |
| `ancestral_eta` | `float` | `0.5` | no | — | 0.0 | 1.0 | Ancestral (SDE) Euler eta for the per-tile denoise |
| `cfg` | `float` | `1.0` | no | — | 1.0 | 20.0 | True CFG scale for the per-tile denoise. 1.0 (single forward) matches the refine character of a round; the negative prompt then reaches the pass only through NAG |
| `num_tiles_override` | `int` | `0` | no | — | 0 | 64 | Force a tile count per round instead of 2**round (0 = automatic). A larger count is the VRAM relief valve: the seam, lead-in and drop rules are unchanged, tiles just own fewer segments each |
| `max_tile_tokens` | `int` | `0` | no | — | 0 | — | Raise a round's tile count until no tile exceeds this projected video token count (0 = off). The finer split never changes the seam geometry |
| `reanchor_each_round` | `bool` | `false` | no | — | — | — | Re-derive the keyframe bag from each round's own stitched output at full canvas density instead of only doubling the carried positions. Costs one extra decode+encode per round; without generated keyframe slots it is the only way to keep the seams from halving in density every round |
| `media_placements` | `list` | `[]` | no | — | — | — | Media conditioning placements, same shape as the generator pipes': [{source: image, index, frame: int\|first\|last, strength}]. Re-applied per tile at tile-LOCAL frame indices; only image sources are supported |
| `decode` | `bool` | `true` | no | — | — | — | Decode to video; false emits the densified latent instead |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `nag_scale` | `float` | `1.0` | no | — | 1.0 | 20.0 | Normalized Attention Guidance scale (1.0 = off). The only channel by which the negative prompt reaches a cfg-1.0 round |
| `nag_tau` | `float` | `3.5` | no | — | 0.1 | 20.0 | NAG norm-clamp threshold |
| `nag_alpha` | `float` | `0.5` | no | — | 0.0 | 1.0 | NAG blend-back weight |

### <a id="pipe-generator-flux"></a>`generator/flux`

Native Flux-family generator (flow matching, embedded guidance)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | Flux model bundle |
| `conditioning` | `CONDITIONING` | yes | yes | Encoded prompt conditioning (per image) |
| `seed` | `SEED` | no | yes | Random seeds |
| `image` | `IMAGE` | no | yes | Input images for img2img mode |
| `MODELS` | `SERVICE` | no | no | Model lifecycle service, to release the idle TE's VRAM |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Generated images |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `mode` | `str` | `"txt2img"` | yes | `txt2img`, `img2img` | — | — | Generation mode |
| `denoise` | `float` | `0.55` | no | — | 0.0 | 1.0 | img2img denoise strength (fraction of the schedule to walk) |
| `img2img_scale` | `float` | `0.0` | no | — | 0.0 | 8.0 | Literal multiplier of the SOURCE image's own (width, height) for the img2img target size, snapped to the model's granularity same as every other path here. 0 (default) is off and leaves the existing resolution-area-match / area-cap behaviour byte-identical -- set this instead of (not alongside) the mode's ``resolution`` config for a true "upscale by Nx of whatever was uploaded" mode. |
| `steps` | `int` | `20` | no | — | 1 | 100 | Denoising steps |
| `guidance` | `float` | `3.5` | no | — | 0.0 | 30.0 | Embedded (distilled) guidance scale |
| `shift` | `float` | — | no | — | — | — | Sigma-shift override (constant-shift variants only) |
| `sampler` | `str` | `"euler"` | no | `euler`, `dpmpp_2m`, `dpmpp_2m_sde`, `dpmpp_3m`, `res_multistep`, `unipc`, `lcm` | — | — | Sampler |
| `resolution` | `str` | `"1024x1024"` | no | — | — | — | Resolution (WxH) |
| `quantity` | `int` | `1` | no | — | 1 | 10 | Number of images |
| `seed` | `int` | `-1` | no | — | -1 | — | Random seed |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `preview` | `bool` | `true` | no | — | — | — | Emit live latent previews to the workbench during sampling |
| `iterate_mode` | `bool` | `false` | no | — | — | — | Iterate mode: resume a cached mid-trajectory latent instead of a cold denoise when a follow-up generation's conditioning barely changed, skipping the steps that already agree. Off by default (needs GPU validation). Only engages on the 'euler' sampler, txt2img (no input image), with APG momentum unset/0 -- every other combination silently falls back to a normal cold run. |
| `spectral_progressive` | `dict` | — | no | — | — | — | Spectral Progressive Diffusion (opt-in prototype): denoise the early, high-sigma steps at a reduced latent resolution and grow to full resolution as the schedule's frequency bands stop being noise-dominated. {'scales': [0.5, 1.0], 'delta': 0.01, 'power_beta': 2.5, 'power_amplitude': 1.0, 'basis': 'fft'\|'dct', 'transitions': null, 'enabled': true} -- usually only 'scales' is worth setting, the rest derive a sensible schedule from it. Off by default (needs GPU validation). Only engages on a constant-shift, 4D-image, txt2img family (Flux2/Klein, Z-Image); every dynamic-mu family (Flux1, Krea-2), video family, or img2img run silently falls back to the normal path. |
| `step_cache` | `dict` | `{}` | no | — | — | — | FBCache step-skipping options, forwarded to NativeGenerator.sample() unmodified: {'rel_threshold': 0.12, 'warmup_steps': 4, 'max_consecutive_skips': 3}. rel_threshold<=0 (default/absent) is off and never wraps the guidance strategy -- byte-identical to leaving this unset. Read directly by FlowMatchGeneratorPipe.build_context, not through the flat step_cache_threshold/warmup_steps/max_skips resolver the Wan/LTX video pipes use (this family has no such resolver). |

### <a id="pipe-generator-img2vid-wan22"></a>`generator/img2vid_wan22`

Native Wan image-to-video generator (concat conditioning, dual-expert)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | Wan model bundle |
| `conditioning` | `CONDITIONING` | yes | yes | Encoded prompt conditioning |
| `image` | `IMAGE` | yes | yes | Start frame for image-to-video |
| `end_image` | `IMAGE` | no | yes | Optional end frame for first+last-frame (FLF) conditioning |
| `seed` | `SEED` | no | yes | Random seeds |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `video` | `VIDEO` | yes | Generated videos |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `mode` | `str` | `"img2vid"` | yes | `img2vid` | — | — | Generation mode |
| `steps` | `int` | `30` | no | — | 1 | 100 | Denoising steps |
| `cfg` | `float` | `5.0` | no | — | 1.0 | 20.0 | True CFG scale |
| `sampler` | `str` | `"unipc"` | no | `euler`, `dpmpp_2m`, `dpmpp_2m_sde`, `dpmpp_3m`, `res_multistep`, `unipc`, `lcm` | — | — | Sampler |
| `resolution` | `str` | `"832x480"` | no | — | — | — | Resolution (WxH) |
| `frames` | `int` | `81` | no | — | 1 | 257 | Number of video frames |
| `fps` | `float` | `16.0` | no | — | 1.0 | 60.0 | Output frame rate |
| `quantity` | `int` | `1` | no | — | 1 | 4 | Number of videos |
| `seed` | `int` | `-1` | no | — | -1 | — | Random seed |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `expert_boundary` | `float` | — | no | — | 0.0 | 1.0 | Dual-expert switch boundary override |
| `expert_switch_step` | `int` | — | no | — | 0 | — | Switch high->low expert at this step (wins over expert_boundary; converted to that step's sigma) |
| `preview` | `bool` | `true` | no | — | — | — | Emit live latent previews (frame 0) to the workbench during sampling |
| `cfg_zero_star` | `bool` | `true` | no | — | — | — | CFG-Zero*: rescale the uncond branch onto cond before extrapolation (free quality correction) |
| `zero_init_steps` | `int` | `0` | no | — | 0 | 100 | CFG-Zero*: return a zero velocity prediction for the first N steps |
| `nag_scale` | `float` | `1.0` | no | — | 1.0 | 20.0 | Normalized Attention Guidance scale (1.0 = off). Injects the negative prompt into cross-attention so it's enforced even at cfg=1.0 (single-forward speed) — set cfg to 1.0 and nag_scale to ~1.1-1.5 for the speed win; NAG stacks with true CFG if both are on. |
| `nag_tau` | `float` | `3.5` | no | — | 0.1 | 20.0 | NAG norm-clamp threshold (paper default 3.5) |
| `nag_alpha` | `float` | `0.5` | no | — | 0.0 | 1.0 | NAG blend-back-toward-positive weight (paper default 0.5) |
| `riflex` | `bool` | `false` | no | — | — | — | RIFLEx: extrapolate video length beyond the model's trained frame count via intrinsic RoPE frequency clamping (default off = byte-identical schedule) |
| `riflex_trained_frames` | `int` | — | no | — | 1 | — | RIFLEx: override the trained latent-frame count (default: the family's known trained length; only used when riflex=true) |
| `apg_eta` | `float` | — | no | — | 0.0 | 1.0 | APG: down-weight of the CFG delta's component parallel to the cond prediction (unset = inherit the model's own sampling_settings, or 1.0 = off / plain CFG if neither sets it; paper explores ~0.0-0.5 to reduce oversaturation at high CFG) |
| `apg_norm_threshold` | `float` | — | no | — | 0.0 | — | APG: cap the guidance delta's norm to this radius (unset = inherit; 0 = off) |
| `apg_momentum` | `float` | — | no | — | -1.0 | 1.0 | APG: reverse-momentum coefficient applied to the guidance delta across steps (unset = inherit; 0 = off; paper uses small negative values, e.g. -0.5) |
| `slg_scale` | `float` | — | no | — | 0.0 | 10.0 | Skip-Layer Guidance: push-away strength from a degraded (blocks-skipped) prediction (unset = inherit the model's own sampling_settings, or 0 = off if neither sets it) |
| `slg_layers` | `str` | — | no | — | — | — | Skip-Layer Guidance: comma-separated transformer block indices to skip in the degraded pass (unset = inherit; empty = off even if slg_scale > 0) |
| `slg_sigma_start` | `float` | — | no | — | 0.0 | 1.0 | Skip-Layer Guidance: window upper (earlier-trajectory) sigma bound (unset = inherit) |
| `slg_sigma_end` | `float` | — | no | — | 0.0 | 1.0 | Skip-Layer Guidance: window lower (later-trajectory) sigma bound (unset = inherit) |
| `sampler_options` | `dict` | `{}` | no | — | — | — | Opaque per-sampler options dict forwarded to the chosen sampler, e.g. {'eta': 0.5} for sampler=euler_sde or {'restart_count': 2, 'restart_strength': 0.3} for sampler=euler_restart. Empty/absent = sampler defaults, no effect on euler/dpmpp_2m/unipc. |
| `step_cache` | `dict` | `{}` | no | — | — | — | FBCache step-skipping options: {'rel_threshold': 0.12, 'warmup_steps': 4, 'max_consecutive_skips': 3}. rel_threshold<=0 (default/absent) is off. Only arch forwards that honor the step_cache kwarg actually skip steps; others ignore it harmlessly. Takes priority over the flat step_cache_threshold/step_cache_warmup_steps/step_cache_max_skips knobs below when non-empty (back-compat with existing presets/callers). |
| `step_cache_threshold` | `float` | `0.0` | no | — | 0.0 | 1.0 | FBCache step-skipping: relative-error threshold below which a step's residual is reused instead of recomputed. 0.0 (default) is off; ~0.08 is a conservative starting point, ~0.15 more aggressive. Only pays off on runs of 15+ steps -- warmup_steps alone already covers shorter runs. Ignored when the 'step_cache' dict above is also set. |
| `step_cache_warmup_steps` | `int` | `4` | no | — | 0 | — | FBCache step-skipping: number of leading steps forced to fully compute (never skipped) before the cache is allowed to kick in. Ignored when the 'step_cache' dict above is also set. |
| `step_cache_max_skips` | `int` | `3` | no | — | 0 | — | FBCache step-skipping: maximum number of consecutive steps the cache may skip before a forced recompute. Ignored when the 'step_cache' dict above is also set. |
| `schedule` | `str` | `""` | no | — | — | — | Sigma schedule key: '' (default) or 'shift' for the model's own shift-based ramp, or any key registered on the schedule registry -- 'beta', 'exponential', 'linear_quadratic', 'manual', 'ltx_dynamic' in core, plus whatever a plugin's 'schedules:' manifest root adds. GET /api/sampling/catalog lists what this instance accepts. Prefer 'manual_sigmas' below over setting this to 'manual' directly -- it also fills schedule_options for you. 'ltx_dynamic' only functions on a pipe that feeds image_seq_len into the schedule builder -- currently the LTX generator pipes. |
| `schedule_options` | `dict` | `{}` | no | — | — | — | Schedule-specific options: beta {'alpha': 0.6, 'beta': 0.6}, exponential {'sigma_min': 1e-3}, linear_quadratic {'threshold_noise': 0.025, 'linear_steps': <int, default steps // 2>}, or ltx_dynamic {'base_shift': 0.95, 'max_shift': 2.05, 'stretch': True, 'terminal': 0.1} (LTX-2.5 defaults). Ignored when schedule is unset. |
| `manual_sigmas` | `str` | `""` | no | — | — | — | Explicit, comma-separated, descending sigma schedule ('1.0, 0.99375, 0.9875, ..., 0.0'; ComfyUI 'ManualSigmas'-style, e.g. a distilled-LoRA refine tail -- see docs/models/ltx.md). Its length IS the step count: overrides 'steps' and any shift/schedule setting outright. Takes priority over 'schedule'/'schedule_options' when non-empty (the default '' is a no-op, byte-identical to not having this knob). |
| `detail_strength` | `float` | — | no | — | -0.3 | 0.3 | Detail-daemon sigma warp strength (unset = inherit the model's own sampling_settings, or 0 = off if neither sets it; expected range -0.3 to 0.3) |
| `detail_start` | `float` | — | no | — | 0.0 | 1.0 | Detail-daemon warp window start (trajectory fraction; unset = inherit) |
| `detail_end` | `float` | — | no | — | 0.0 | 1.0 | Detail-daemon warp window end (trajectory fraction; unset = inherit) |

### <a id="pipe-generator-krea2"></a>`generator/krea2`

Native Krea-2 generator (flow matching, true CFG; turbo defaults to cfg=1)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | Krea-2 model bundle |
| `conditioning` | `CONDITIONING` | yes | yes | Encoded prompt conditioning (per image) |
| `seed` | `SEED` | no | yes | Random seeds |
| `image` | `IMAGE` | no | yes | Input images for img2img mode |
| `MODELS` | `SERVICE` | no | no | Model lifecycle service, to release the idle TE's VRAM before sampling |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Generated images |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `mode` | `str` | `"txt2img"` | yes | `txt2img`, `img2img` | — | — | Generation mode |
| `denoise` | `float` | `0.55` | no | — | 0.0 | 1.0 | img2img denoise strength (fraction of the schedule to walk) |
| `img2img_scale` | `float` | `0.0` | no | — | 0.0 | 8.0 | Literal multiplier of the SOURCE image's own (width, height) for the img2img target size, snapped to the model's granularity same as every other path here. 0 (default) is off and leaves the existing resolution-area-match / area-cap behaviour byte-identical -- set this instead of (not alongside) the mode's ``resolution`` config for a true "upscale by Nx of whatever was uploaded" mode. |
| `steps` | `int` | `8` | no | — | 1 | 100 | Denoising steps (turbo default 8) |
| `guidance` | `float` | `1.0` | no | — | 1.0 | 15.0 | CFG scale. 1.0 = single conditional-only forward (turbo/distilled default, byte-identical to the old NoCFG path); above 1.0 runs a real negative-conditioned forward pass each step -- needed for a raw/base checkpoint, optional as an experiment on the distilled checkpoint at higher step counts. |
| `mu_schedule` | `str` | `"fixed"` | no | `fixed`, `dynamic` | — | — | Sigma-schedule mu source: 'fixed' pins the official turbo schedule (fixed_mu=1.15); 'dynamic' switches to the resolution-anchored mu interpolation for a raw/base (non-distilled) checkpoint (see docs/models/krea2.md). |
| `sampler` | `str` | `"euler"` | no | `euler`, `dpmpp_2m`, `dpmpp_2m_sde`, `dpmpp_3m`, `res_multistep`, `unipc`, `lcm`, `er_sde` | — | — | Sampler |
| `resolution` | `str` | `"1024x1024"` | no | — | — | — | Resolution (WxH) |
| `quantity` | `int` | `1` | no | — | 1 | 10 | Number of images |
| `seed` | `int` | `-1` | no | — | -1 | — | Random seed |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `preview` | `bool` | `true` | no | — | — | — | Emit live latent previews to the workbench during sampling |
| `nag_scale` | `float` | `1.0` | no | — | 1.0 | 20.0 | Normalized Attention Guidance scale (1.0 = off). Injects the negative prompt into the joint attention so it's enforced even at guidance=1.0 (single-forward turbo speed) — set nag_scale to ~1.1-1.5 for real negative-prompt influence without a second forward pass. Redundant with (and unvalidated alongside) real CFG (guidance > 1.0), which already runs a full negative-conditioned pass -- leave at 1.0 on a CFG profile. |
| `nag_tau` | `float` | `3.5` | no | — | 0.1 | 20.0 | NAG norm-clamp threshold (paper default 3.5) |
| `nag_alpha` | `float` | `0.5` | no | — | 0.0 | 1.0 | NAG blend-back-toward-positive weight (paper default 0.5) |
| `refine_tail` | `str` | `""` | no | ``, `subtle`, `balanced`, `strong` | — | — | Whole-frame enhance (img2img only): walk only the tail of the official fixed-mu 8-step Euler grid instead of a denoise-truncated schedule. '' = off, 'subtle'/'balanced'/'strong' = the last 2/3/4 grid steps. |
| `schedule` | `str` | `""` | no | — | — | — | Sigma schedule key: '' (default) or 'shift' for the model's own shift-based ramp, or any key registered on the schedule registry -- 'beta', 'exponential', 'linear_quadratic', 'manual', 'ltx_dynamic' in core, plus whatever a plugin's 'schedules:' manifest root adds. GET /api/sampling/catalog lists what this instance accepts. Prefer 'manual_sigmas' below over setting this to 'manual' directly -- it also fills schedule_options for you. 'ltx_dynamic' only functions on a pipe that feeds image_seq_len into the schedule builder -- currently the LTX generator pipes. |
| `schedule_options` | `dict` | `{}` | no | — | — | — | Schedule-specific options: beta {'alpha': 0.6, 'beta': 0.6}, exponential {'sigma_min': 1e-3}, linear_quadratic {'threshold_noise': 0.025, 'linear_steps': <int, default steps // 2>}, or ltx_dynamic {'base_shift': 0.95, 'max_shift': 2.05, 'stretch': True, 'terminal': 0.1} (LTX-2.5 defaults). Ignored when schedule is unset. |
| `manual_sigmas` | `str` | `""` | no | — | — | — | Explicit, comma-separated, descending sigma schedule ('1.0, 0.99375, 0.9875, ..., 0.0'; ComfyUI 'ManualSigmas'-style, e.g. a distilled-LoRA refine tail -- see docs/models/ltx.md). Its length IS the step count: overrides 'steps' and any shift/schedule setting outright. Takes priority over 'schedule'/'schedule_options' when non-empty (the default '' is a no-op, byte-identical to not having this knob). |
| `detail_strength` | `float` | — | no | — | -0.3 | 0.3 | Detail-daemon sigma warp strength (unset = inherit the model's own sampling_settings, or 0 = off if neither sets it; expected range -0.3 to 0.3) |
| `detail_start` | `float` | — | no | — | 0.0 | 1.0 | Detail-daemon warp window start (trajectory fraction; unset = inherit) |
| `detail_end` | `float` | — | no | — | 0.0 | 1.0 | Detail-daemon warp window end (trajectory fraction; unset = inherit) |
| `step_cache` | `dict` | `{}` | no | — | — | — | FBCache step-skipping options, forwarded to NativeGenerator.sample() unmodified: {'rel_threshold': 0.12, 'warmup_steps': 4, 'max_consecutive_skips': 3}. rel_threshold<=0 (default/absent) is off and never wraps the guidance strategy -- byte-identical to leaving this unset. Read directly by FlowMatchGeneratorPipe.build_context, not through the flat step_cache_threshold/warmup_steps/max_skips resolver the Wan/LTX video pipes use (this family has no such resolver). |

### <a id="pipe-generator-maya"></a>`generator/maya`

Maya text-to-speech generation with emotional expressions

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | Loaded Maya model with all components |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `audio` | `AUDIO` | yes | Generated speech audio file |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `text` | `str` | `"Hello!"` | yes | — | — | — | Text to synthesize into speech |
| `voice_description` | `str` | `""` | no | — | — | — | Custom voice description |
| `voice_age` | `str` | `"30s"` | no | — | — | — | Voice age range |
| `voice_gender` | `str` | `"male"` | no | `male`, `female` | — | — | Voice gender |
| `voice_pitch` | `str` | `"medium"` | no | `low`, `medium-low`, `medium`, `medium-high`, `high` | — | — | Voice pitch level |
| `voice_accent` | `str` | `"american"` | no | — | — | — | Voice accent |
| `voice_warmth` | `float` | `0.5` | no | — | 0.0 | 1.0 | Voice warmth (0-1) |
| `temperature` | `float` | `0.4` | no | — | 0.1 | 1.0 | Generation temperature |
| `top_p` | `float` | `0.9` | no | — | 0.5 | 1.0 | Nucleus sampling top-p |
| `repetition_penalty` | `float` | `1.1` | no | — | 1.0 | 2.0 | Repetition penalty |
| `max_new_tokens` | `int` | `2048` | no | — | 256 | 4096 | Max tokens to generate |
| `min_new_tokens` | `int` | `28` | no | — | 1 | 256 | Min tokens to generate |
| `seed` | `int` | `-1` | no | — | — | — | Random seed (-1 for random) |
| `sample_rate` | `int` | `24000` | no | `24000` | — | — | Audio sample rate |

### <a id="pipe-generator-qwen"></a>`generator/qwen`

Native Qwen-Image generator (flow matching, true CFG)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | Qwen-Image model bundle |
| `conditioning` | `CONDITIONING` | yes | yes | Encoded prompt conditioning (per image) |
| `seed` | `SEED` | no | yes | Random seeds |
| `image` | `IMAGE` | no | yes | Input images for img2img mode |
| `MODELS` | `SERVICE` | no | no | Model lifecycle service, to release the idle TE's VRAM |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Generated images |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `mode` | `str` | `"txt2img"` | yes | `txt2img`, `img2img`, `edit` | — | — | Generation mode |
| `denoise` | `float` | `0.55` | no | — | 0.0 | 1.0 | img2img denoise strength (fraction of the schedule to walk) |
| `img2img_scale` | `float` | `0.0` | no | — | 0.0 | 8.0 | Literal multiplier of the SOURCE image's own (width, height) for the img2img target size, snapped to the model's granularity same as every other path here. 0 (default) is off and leaves the existing resolution-area-match / area-cap behaviour byte-identical -- set this instead of (not alongside) the mode's ``resolution`` config for a true "upscale by Nx of whatever was uploaded" mode. |
| `steps` | `int` | `20` | no | — | 1 | 100 | Denoising steps |
| `guidance` | `float` | `4.0` | no | — | 0.0 | 30.0 | True CFG scale (Qwen true_cfg_scale) |
| `shift` | `float` | — | no | — | — | — | Sigma-shift override; blank -> spec default (1.15) |
| `sampler` | `str` | `"euler"` | no | `euler`, `dpmpp_2m`, `dpmpp_2m_sde`, `dpmpp_3m`, `res_multistep`, `unipc`, `lcm` | — | — | Sampler |
| `resolution` | `str` | `"1024x1024"` | no | — | — | — | Resolution (WxH) |
| `quantity` | `int` | `1` | no | — | 1 | 10 | Number of images |
| `seed` | `int` | `-1` | no | — | -1 | — | Random seed |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `preview` | `bool` | `true` | no | — | — | — | Emit live latent previews to the workbench during sampling |
| `iterate_mode` | `bool` | `false` | no | — | — | — | Iterate mode: resume a cached mid-trajectory latent instead of a cold denoise when a follow-up generation's conditioning barely changed, skipping the steps that already agree. Off by default (needs GPU validation). Only engages on the 'euler' sampler, txt2img (no input image), with APG momentum unset/0 -- every other combination silently falls back to a normal cold run. |

### <a id="pipe-generator-sdxl"></a>`generator/sdxl`

SDXL-optimized generator pipe

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | AI model for image generation |
| `conditioning` | `CONDITIONING` | yes | yes | Encoded prompt conditioning |
| `seed` | `SEED` | no | yes | Random seeds for generation |
| `image` | `IMAGE` | no | yes | Input image for img2img mode |
| `mask` | `IMAGE` | no | no | Mask image for inpainting mode |
| `controlnet` | `CONTROLNET` | no | yes | ControlNet models for guided generation |
| `control_image` | `IMAGE` | no | yes | Control images for ControlNet |
| `ASSETS` | `SERVICE` | no | no | Asset fetcher, to fetch the inpaint head before a masked generation |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Generated images |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `mode` | `str` | `"txt2img"` | yes | `txt2img`, `img2img` | — | — | Generation mode |
| `model` | `str` | — | no | — | — | — | Model identifier to use for generation |
| `vae` | `str` | — | no | — | — | — | VAE model identifier |
| `steps` | `int` | `25` | no | — | 1 | 150 | Number of denoising steps |
| `cfg` | `float` | `6.0` | no | — | 0.0 | 30.0 | Classifier Free Guidance scale |
| `sampler` | `str` | `"DPMPP_2M"` | no | `EULER`, `EULER_A`, `HEUN`, `DPM2`, `DPM2_A`, `LMS`, `DPMPP_2S_A`, `DPMPP_SDE`, `DPMPP_2M`, `DPMPP_2M_SDE`, `DPMPP_3M_SDE`, `LCM` | — | — | Sampling algorithm to use |
| `scheduler` | `str` | `"karras"` | no | `normal`, `karras`, `exponential`, `sgm_uniform`, `simple`, `ddim_uniform` | — | — | Noise schedule type to use |
| `seed` | `int` | `-1` | no | — | -1 | — | Random seed (-1 for random) |
| `resolution` | `str` | `"1024x1024"` | no | — | — | — | Output resolution (WxH) |
| `quantity` | `int` | `1` | no | — | 1 | 10 | Number of images to generate |
| `clip_skip` | `int` | `2` | no | — | 1 | 12 | CLIP skip value |
| `denoise` | `float` | `0.8` | no | — | 0.0 | 1.0 | Denoising strength for img2img |
| `embeddings` | `dict` | `{}` | no | — | — | — | Embeddings configuration |
| `guidance_rescale` | `float` | `0.0` | no | — | 0.0 | 1.0 | Guidance rescale factor (0.0 = disabled, 0.5-0.7 = good for realistic at high CFG) |
| `inpaint_mode` | `bool` | `false` | no | — | — | — | Enable inpainting mode (requires mask input) |
| `mask_blur` | `int` | `4` | no | — | 0 | 20 | Blur radius for mask edges (smoother blending) |

### <a id="pipe-generator-seedvr2"></a>`generator/seedvr2`

Native SeedVR2 one-step restoration upscaler (no text encoder, no CFG, no denoise loop)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `image` | `IMAGE` | no | yes | Low-res images to restore/upscale |
| `video` | `VIDEO` | no | yes | Low-res video(s) to restore/upscale |
| `model` | `MODEL` | yes | no | SeedVR2 model bundle |
| `seed` | `SEED` | no | yes | Per-image seeds |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Restored / upscaled images |
| `video` | `VIDEO` | yes | Restored / upscaled video(s) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `scale` | `float` | `2.0` | no | — | 1.0 | 8.0 | Upscale factor (output area = scale^2 x input); ignored when target_short_side > 0 |
| `target_short_side` | `int` | `0` | no | — | 0 | 4096 | Target short-side in px (0 -> use scale). When >0 this wins over scale. |
| `color_correction` | `str` | `"wavelet"` | no | `wavelet`, `adain`, `none` | — | — | Match output colors back to the resized input |
| `latent_noise_scale` | `float` | `0.0` | no | — | 0.0 | 1.0 | Forward-noise the conditioning latent (0 = SeedVR2-faithful; softens artifacts) |
| `input_noise_scale` | `float` | `0.0` | no | — | 0.0 | 1.0 | Small gaussian on the input pixels before encode (0 = off) |
| `seed` | `int` | `-1` | no | — | -1 | — | Seed (-1 -> per-image random) |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `tile_size` | `int` | `2048` | no | — | 256 | 2048 | VAE encode/decode tile size ceiling (px); auto-tiles by live VRAM under this |
| `tile_overlap` | `int` | `256` | no | — | 0 | 512 | VAE encode/decode tile overlap ceiling (px) |
| `batch_size` | `int` | `0` | no | — | 0 | 49 | Frames per temporal batch (snapped to the VAE's 4n+1 lattice: 1,5,9,…). The temporal VRAM knob — one batch is on the GPU at a time. 0 = auto-size to ~92% of live free VRAM at the output resolution (recommended); the OOM ladder halves it and re-runs if a batch ever exceeds real VRAM. |
| `temporal_overlap` | `int` | `0` | no | — | 0 | 16 | Overlapping frames cross-faded between consecutive batches (0 = hard cut) |
| `prepend_frames` | `int` | `0` | no | — | 0 | 16 | Reversed head frames prepended before upscaling to reduce clip-start artifacts (auto-removed from the output) |
| `uniform_batch_size` | `bool` | `true` | no | — | — | — | Pad a short final batch up to the full batch size (avoids seam artifacts) |
| `keep_audio` | `bool` | `true` | no | — | — | — | Mux the source video's audio track into the upscaled output |

### <a id="pipe-generator-trellis2"></a>`generator/trellis2`

Native TRELLIS.2 generator (single image to a textured GLB mesh)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | TRELLIS.2 model bundle |
| `image` | `IMAGE` | yes | yes | Source image(s) to reconstruct |
| `seed` | `SEED` | no | yes | Random seeds, one per image |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `mesh` | `MESH` | yes | Paths to the generated .glb files |
| `seed` | `SEED` | yes | Seed each mesh was generated with |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `seed` | `int` | `-1` | no | — | -1 | — | Random seed; -1 draws a fresh one per image |
| `remove_background` | `bool` | `false` | no | — | — | — | Crop and centre the subject before reconstruction. An image with a transparent background uses its own alpha; an opaque one needs the loader's matting model and fails with an explanation without it. |
| `decimation_target` | `int` | `100000` | no | — | 5000 | 1000000 | Face budget the exported mesh is decimated to. The UV unwrap is CPU-bound and scales worse than linearly, so this dominates export time above ~100k. |
| `texture_size` | `int` | `2048` | no | `1024`, `2048`, `4096` | — | — | Edge length of the baked PBR texture maps |
| `max_num_tokens` | `int` | `49152` | no | — | 4096 | 262144 | Token budget for the cascade's high-resolution shape pass. A shape too detailed for the tier is decoded at a coarser one instead of failing. |
| `project_to_source` | `bool` | `false` | no | — | — | — | Push baked texels back onto the pre-decimation surface. More accurate and much slower — the query has no acceleration structure on CPU. |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `sparse_structure_steps` | `int` | `12` | no | — | 1 | 50 | Sampling steps for the sparse-structure stage |
| `sparse_structure_guidance_strength` | `float` | `7.5` | no | — | 0.0 | 10.0 | Guidance strength for the sparse-structure stage |
| `sparse_structure_guidance_rescale` | `float` | `0.7` | no | — | 0.0 | 1.0 | Guidance rescale for the sparse-structure stage |
| `sparse_structure_rescale_t` | `float` | `5.0` | no | — | 1.0 | 6.0 | Rescale t for the sparse-structure stage |
| `shape_steps` | `int` | `12` | no | — | 1 | 50 | Sampling steps for the shape stage |
| `shape_guidance_strength` | `float` | `7.5` | no | — | 0.0 | 10.0 | Guidance strength for the shape stage |
| `shape_guidance_rescale` | `float` | `0.5` | no | — | 0.0 | 1.0 | Guidance rescale for the shape stage |
| `shape_rescale_t` | `float` | `3.0` | no | — | 1.0 | 6.0 | Rescale t for the shape stage |
| `texture_steps` | `int` | `12` | no | — | 1 | 50 | Sampling steps for the PBR texture stage |
| `texture_guidance_strength` | `float` | `1.0` | no | — | 0.0 | 10.0 | Guidance strength for the PBR texture stage |
| `texture_guidance_rescale` | `float` | `0.0` | no | — | 0.0 | 1.0 | Guidance rescale for the PBR texture stage |
| `texture_rescale_t` | `float` | `3.0` | no | — | 1.0 | 6.0 | Rescale t for the PBR texture stage |

### <a id="pipe-generator-txt2vid-ltx"></a>`generator/txt2vid_ltx`

Native LTX-2/2.3 text-to-video generator (video-only)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | LTX model bundle |
| `conditioning` | `CONDITIONING` | yes | yes | Encoded prompt conditioning |
| `seed` | `SEED` | no | yes | Random seeds |
| `initial_latent` | `LATENT` | no | yes | Seed latent(s) for a refine pass, one per seed; absent -> ordinary txt2vid from pure noise |
| `audio_source` | `VIDEO` | no | no | Existing video whose audio track is preserved into this pipe's own encode (standalone upscale mode has no audio latents of its own -- see module docstring). A fallback only: ignored if this call ever produces its own generated audio |
| `source_frame_count` | `INT` | no | no | Original (pre-temporal-padding) frame count of the standalone-upscale source video, from latent_upscaler/ltx's 'source_frame_count' output -- used to trim the padded duplicate tail frames from this pipe's own decoded output before mux. Absent -> no trim (in-flow two-stage, or a 'video' input that was already on-grid) |
| `MODELS` | `SERVICE` | no | no | Model lifecycle service, to release the idle TE's host RAM |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `video` | `VIDEO` | yes | Generated videos (empty when decode=false) |
| `latent` | `LATENT` | yes | Raw per-seed latents (only populated when decode=false) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `mode` | `str` | `"txt2vid"` | yes | `txt2vid` | — | — | Generation mode |
| `decode` | `bool` | `true` | no | — | — | — | Decode to video; set false to emit the raw latent for a downstream latent_upscaler/refine stage instead |
| `refine_sigmas` | `str` | `""` | no | — | — | — | Explicit sigma schedule for a refine pass, comma-separated, descending, used VERBATIM (unlike manual_sigmas, the head is not forced to 1.0) -- required when 'initial_latent' is connected |
| `steps` | `int` | `24` | no | — | 1 | 100 | Denoising steps |
| `cfg` | `float` | `4.0` | no | — | 1.0 | 20.0 | True CFG scale |
| `sampler` | `str` | `"euler"` | no | `euler`, `dpmpp_2m`, `dpmpp_2m_sde`, `dpmpp_3m`, `res_multistep`, `unipc`, `lcm`, `euler_ancestral`, `euler_ancestral_cfg_pp`, `euler_cfg_pp` | — | — | Sampler. 'euler_ancestral' is LTX-2.5's stage-1 sampler (stochastic, eta=1.0 by default via sampler_options) -- pair it with schedule='ltx_dynamic' for the matching resolution-aware sigma shift. |
| `resolution` | `str` | `"768x512"` | no | — | — | — | Resolution (WxH) |
| `frames` | `int` | `49` | no | — | 1 | 1001 | Number of video frames (must be 1 + 8*k; up to ~40s at 25fps) |
| `fps` | `float` | — | no | — | 1.0 | 60.0 | Output frame rate (unset -> 25.0, or the audio_source video's own detected rate when one is connected, so muxed audio stays in sync) |
| `quantity` | `int` | `1` | no | — | 1 | 4 | Number of videos |
| `seed` | `int` | `-1` | no | — | -1 | — | Random seed |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `preview` | `bool` | `true` | no | — | — | — | Emit live latent previews (frame 0) to the workbench during sampling |
| `cfg_zero_star` | `bool` | `true` | no | — | — | — | CFG-Zero*: rescale the uncond branch onto cond before extrapolation (free quality correction) |
| `zero_init_steps` | `int` | `0` | no | — | 0 | 100 | CFG-Zero*: return a zero velocity prediction for the first N steps |
| `nag_scale` | `float` | `1.0` | no | — | 1.0 | 20.0 | Normalized Attention Guidance scale (1.0 = off). Injects the negative prompt into cross-attention so it's enforced even at cfg=1.0 (single-forward speed) — set cfg to 1.0 and nag_scale to ~1.1-1.5 for the speed win; NAG stacks with true CFG if both are on. |
| `nag_tau` | `float` | `3.5` | no | — | 0.1 | 20.0 | NAG norm-clamp threshold (paper default 3.5) |
| `nag_alpha` | `float` | `0.5` | no | — | 0.0 | 1.0 | NAG blend-back-toward-positive weight (paper default 0.5) |
| `apg_eta` | `float` | — | no | — | 0.0 | 1.0 | APG: down-weight of the CFG delta's component parallel to the cond prediction (unset = inherit the model's own sampling_settings, or 1.0 = off / plain CFG if neither sets it; paper explores ~0.0-0.5 to reduce oversaturation at high CFG) |
| `apg_norm_threshold` | `float` | — | no | — | 0.0 | — | APG: cap the guidance delta's norm to this radius (unset = inherit; 0 = off) |
| `apg_momentum` | `float` | — | no | — | -1.0 | 1.0 | APG: reverse-momentum coefficient applied to the guidance delta across steps (unset = inherit; 0 = off; paper uses small negative values, e.g. -0.5) |
| `sampler_options` | `dict` | `{}` | no | — | — | — | Opaque per-sampler options dict forwarded to the chosen sampler, e.g. {'eta': 0.5} for sampler=euler_sde or {'restart_count': 2, 'restart_strength': 0.3} for sampler=euler_restart. Empty/absent = sampler defaults, no effect on euler/dpmpp_2m/unipc. |
| `step_cache` | `dict` | `{}` | no | — | — | — | FBCache step-skipping options: {'rel_threshold': 0.12, 'warmup_steps': 4, 'max_consecutive_skips': 3}. rel_threshold<=0 (default/absent) is off. Only arch forwards that honor the step_cache kwarg actually skip steps; others ignore it harmlessly. Takes priority over the flat step_cache_threshold/step_cache_warmup_steps/step_cache_max_skips knobs below when non-empty (back-compat with existing presets/callers). |
| `step_cache_threshold` | `float` | `0.0` | no | — | 0.0 | 1.0 | FBCache step-skipping: relative-error threshold below which a step's residual is reused instead of recomputed. 0.0 (default) is off; ~0.08 is a conservative starting point, ~0.15 more aggressive. Only pays off on runs of 15+ steps -- warmup_steps alone already covers shorter runs. Ignored when the 'step_cache' dict above is also set. |
| `step_cache_warmup_steps` | `int` | `4` | no | — | 0 | — | FBCache step-skipping: number of leading steps forced to fully compute (never skipped) before the cache is allowed to kick in. Ignored when the 'step_cache' dict above is also set. |
| `step_cache_max_skips` | `int` | `3` | no | — | 0 | — | FBCache step-skipping: maximum number of consecutive steps the cache may skip before a forced recompute. Ignored when the 'step_cache' dict above is also set. |
| `freeinit_iterations` | `int` | `0` | no | — | 0 | 5 | FreeInit: extra full denoise passes that re-noise the previous pass's clean result in 3D frequency space (keep its low frequencies, replace its high frequencies with fresh noise) to reduce video temporal flicker from the noise-init gap. 0 = off (byte-identical single pass). Each iteration re-pays the full sampling cost, so 1-2 is the practical default even though the paper explores 3-5. |
| `freeinit_cutoff` | `float` | `0.25` | no | — | 0.01 | 1.0 | FreeInit: normalized Butterworth HALF-POWER cutoff frequency (paper default ~0.25) -- at d == cutoff the mask value is exactly 0.5, not a hard boundary, so even cutoff=1.0 still attenuates (roughly halves) the cardinal-axis Nyquist bin and attenuates diagonal/high-frequency bins much further; only cutoff -> 0 keeps solely the DC (mean) component as 'low' and cutoff -> infinity keeps everything. |
| `freeinit_order` | `int` | `4` | no | — | 1 | 8 | FreeInit: Butterworth filter order — higher is a steeper cutoff (closer to a hard low-pass), lower is a gentler roll-off. |
| `schedule` | `str` | `""` | no | — | — | — | Sigma schedule key: '' (default) or 'shift' for the model's own shift-based ramp, or any key registered on the schedule registry -- 'beta', 'exponential', 'linear_quadratic', 'manual', 'ltx_dynamic' in core, plus whatever a plugin's 'schedules:' manifest root adds. GET /api/sampling/catalog lists what this instance accepts. Prefer 'manual_sigmas' below over setting this to 'manual' directly -- it also fills schedule_options for you. 'ltx_dynamic' only functions on a pipe that feeds image_seq_len into the schedule builder -- currently the LTX generator pipes. |
| `schedule_options` | `dict` | `{}` | no | — | — | — | Schedule-specific options: beta {'alpha': 0.6, 'beta': 0.6}, exponential {'sigma_min': 1e-3}, linear_quadratic {'threshold_noise': 0.025, 'linear_steps': <int, default steps // 2>}, or ltx_dynamic {'base_shift': 0.95, 'max_shift': 2.05, 'stretch': True, 'terminal': 0.1} (LTX-2.5 defaults). Ignored when schedule is unset. |
| `manual_sigmas` | `str` | `""` | no | — | — | — | Explicit, comma-separated, descending sigma schedule ('1.0, 0.99375, 0.9875, ..., 0.0'; ComfyUI 'ManualSigmas'-style, e.g. a distilled-LoRA refine tail -- see docs/models/ltx.md). Its length IS the step count: overrides 'steps' and any shift/schedule setting outright. Takes priority over 'schedule'/'schedule_options' when non-empty (the default '' is a no-op, byte-identical to not having this knob). |
| `detail_strength` | `float` | — | no | — | -0.3 | 0.3 | Detail-daemon sigma warp strength (unset = inherit the model's own sampling_settings, or 0 = off if neither sets it; expected range -0.3 to 0.3) |
| `detail_start` | `float` | — | no | — | 0.0 | 1.0 | Detail-daemon warp window start (trajectory fraction; unset = inherit) |
| `detail_end` | `float` | — | no | — | 0.0 | 1.0 | Detail-daemon warp window end (trajectory fraction; unset = inherit) |
| `quality_mode` | `bool` | `false` | no | — | — | — | Quality recipe (full model, MultiModalGuider): overrides CFG/sampler with the Lightricks first-party single-pass recipe (cfg 3.0, stg 1.0, rescale 0.7, modality 3.0, 30 steps, euler). Mutually exclusive with Distilled recipe. |
| `quality_cfg` | `float` | — | no | — | 1.0 | 20.0 | Quality mode: CFG scale for the video modality (default 3.0) |
| `quality_stg` | `float` | — | no | — | 0.0 | 10.0 | Quality mode: STG (spatio-temporal guidance) scale for video (default 1.0; 0 = off, skips the perturbed forward) |
| `quality_rescale` | `float` | — | no | — | 0.0 | 1.0 | Quality mode: std-preserving rescale blend (default 0.7; 0 = off) |
| `quality_modality` | `float` | — | no | — | 1.0 | 20.0 | Quality mode: cross-modal guidance scale (default 3.0; 1.0 = off, skips the modality-off forward -- only active with an audio track) |
| `quality_stg_blocks` | `str` | — | no | — | — | — | Quality mode: comma-separated transformer block indices for STG perturbation (default '28' for LTX-2.3) |
| `quality_distilled_strength` | `float` | — | no | — | 0.0 | 2.0 | Quality mode with distilled LoRA: LoRA strength override (ComfyUI F-pass uses 0.2; default 0.2 when quality_mode is on and a LoRA is loaded). Not applied automatically -- just an ai_hint. |

### <a id="pipe-generator-txt2vid-wan22"></a>`generator/txt2vid_wan22`

Native Wan text-to-video generator (dual-expert flow matching)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | Wan model bundle |
| `conditioning` | `CONDITIONING` | yes | yes | Encoded prompt conditioning |
| `seed` | `SEED` | no | yes | Random seeds |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `video` | `VIDEO` | yes | Generated videos |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `mode` | `str` | `"txt2vid"` | yes | `txt2vid` | — | — | Generation mode |
| `steps` | `int` | `30` | no | — | 1 | 100 | Denoising steps |
| `cfg` | `float` | `5.0` | no | — | 1.0 | 20.0 | True CFG scale |
| `sampler` | `str` | `"unipc"` | no | `euler`, `dpmpp_2m`, `dpmpp_2m_sde`, `dpmpp_3m`, `res_multistep`, `unipc`, `lcm` | — | — | Sampler |
| `expert_boundary` | `float` | — | no | — | 0.0 | 1.0 | Dual-expert switch boundary override (sigma fraction) |
| `expert_switch_step` | `int` | — | no | — | 0 | — | Switch high->low expert at this step (wins over expert_boundary; converted to that step's sigma) |
| `cfg_zero_star` | `bool` | `true` | no | — | — | — | CFG-Zero*: rescale the uncond branch onto cond before extrapolation (free quality correction) |
| `zero_init_steps` | `int` | `0` | no | — | 0 | 100 | CFG-Zero*: return a zero velocity prediction for the first N steps |
| `nag_scale` | `float` | `1.0` | no | — | 1.0 | 20.0 | Normalized Attention Guidance scale (1.0 = off). Injects the negative prompt into cross-attention so it's enforced even at cfg=1.0 (single-forward speed) — set cfg to 1.0 and nag_scale to ~1.1-1.5 for the speed win; NAG stacks with true CFG if both are on. |
| `nag_tau` | `float` | `3.5` | no | — | 0.1 | 20.0 | NAG norm-clamp threshold (paper default 3.5) |
| `nag_alpha` | `float` | `0.5` | no | — | 0.0 | 1.0 | NAG blend-back-toward-positive weight (paper default 0.5) |
| `riflex` | `bool` | `false` | no | — | — | — | RIFLEx: extrapolate video length beyond the model's trained frame count via intrinsic RoPE frequency clamping (default off = byte-identical schedule) |
| `riflex_trained_frames` | `int` | — | no | — | 1 | — | RIFLEx: override the trained latent-frame count (default: the family's known trained length; only used when riflex=true) |
| `apg_eta` | `float` | — | no | — | 0.0 | 1.0 | APG: down-weight of the CFG delta's component parallel to the cond prediction (unset = inherit the model's own sampling_settings, or 1.0 = off / plain CFG if neither sets it; paper explores ~0.0-0.5 to reduce oversaturation at high CFG) |
| `apg_norm_threshold` | `float` | — | no | — | 0.0 | — | APG: cap the guidance delta's norm to this radius (unset = inherit; 0 = off) |
| `apg_momentum` | `float` | — | no | — | -1.0 | 1.0 | APG: reverse-momentum coefficient applied to the guidance delta across steps (unset = inherit; 0 = off; paper uses small negative values, e.g. -0.5) |
| `slg_scale` | `float` | — | no | — | 0.0 | 10.0 | Skip-Layer Guidance: push-away strength from a degraded (blocks-skipped) prediction (unset = inherit the model's own sampling_settings, or 0 = off if neither sets it) |
| `slg_layers` | `str` | — | no | — | — | — | Skip-Layer Guidance: comma-separated transformer block indices to skip in the degraded pass (unset = inherit; empty = off even if slg_scale > 0) |
| `slg_sigma_start` | `float` | — | no | — | 0.0 | 1.0 | Skip-Layer Guidance: window upper (earlier-trajectory) sigma bound (unset = inherit) |
| `slg_sigma_end` | `float` | — | no | — | 0.0 | 1.0 | Skip-Layer Guidance: window lower (later-trajectory) sigma bound (unset = inherit) |
| `sampler_options` | `dict` | `{}` | no | — | — | — | Opaque per-sampler options dict forwarded to the chosen sampler, e.g. {'eta': 0.5} for sampler=euler_sde or {'restart_count': 2, 'restart_strength': 0.3} for sampler=euler_restart. Empty/absent = sampler defaults, no effect on euler/dpmpp_2m/unipc. |
| `step_cache` | `dict` | `{}` | no | — | — | — | FBCache step-skipping options: {'rel_threshold': 0.12, 'warmup_steps': 4, 'max_consecutive_skips': 3}. rel_threshold<=0 (default/absent) is off. Only arch forwards that honor the step_cache kwarg actually skip steps; others ignore it harmlessly. Takes priority over the flat step_cache_threshold/step_cache_warmup_steps/step_cache_max_skips knobs below when non-empty (back-compat with existing presets/callers). |
| `step_cache_threshold` | `float` | `0.0` | no | — | 0.0 | 1.0 | FBCache step-skipping: relative-error threshold below which a step's residual is reused instead of recomputed. 0.0 (default) is off; ~0.08 is a conservative starting point, ~0.15 more aggressive. Only pays off on runs of 15+ steps -- warmup_steps alone already covers shorter runs. Ignored when the 'step_cache' dict above is also set. |
| `step_cache_warmup_steps` | `int` | `4` | no | — | 0 | — | FBCache step-skipping: number of leading steps forced to fully compute (never skipped) before the cache is allowed to kick in. Ignored when the 'step_cache' dict above is also set. |
| `step_cache_max_skips` | `int` | `3` | no | — | 0 | — | FBCache step-skipping: maximum number of consecutive steps the cache may skip before a forced recompute. Ignored when the 'step_cache' dict above is also set. |
| `freeinit_iterations` | `int` | `0` | no | — | 0 | 5 | FreeInit: extra full denoise passes that re-noise the previous pass's clean result in 3D frequency space (keep its low frequencies, replace its high frequencies with fresh noise) to reduce video temporal flicker from the noise-init gap. 0 = off (byte-identical single pass). Each iteration re-pays the full sampling cost, so 1-2 is the practical default even though the paper explores 3-5. |
| `freeinit_cutoff` | `float` | `0.25` | no | — | 0.01 | 1.0 | FreeInit: normalized Butterworth HALF-POWER cutoff frequency (paper default ~0.25) -- at d == cutoff the mask value is exactly 0.5, not a hard boundary, so even cutoff=1.0 still attenuates (roughly halves) the cardinal-axis Nyquist bin and attenuates diagonal/high-frequency bins much further; only cutoff -> 0 keeps solely the DC (mean) component as 'low' and cutoff -> infinity keeps everything. |
| `freeinit_order` | `int` | `4` | no | — | 1 | 8 | FreeInit: Butterworth filter order — higher is a steeper cutoff (closer to a hard low-pass), lower is a gentler roll-off. |
| `schedule` | `str` | `""` | no | — | — | — | Sigma schedule key: '' (default) or 'shift' for the model's own shift-based ramp, or any key registered on the schedule registry -- 'beta', 'exponential', 'linear_quadratic', 'manual', 'ltx_dynamic' in core, plus whatever a plugin's 'schedules:' manifest root adds. GET /api/sampling/catalog lists what this instance accepts. Prefer 'manual_sigmas' below over setting this to 'manual' directly -- it also fills schedule_options for you. 'ltx_dynamic' only functions on a pipe that feeds image_seq_len into the schedule builder -- currently the LTX generator pipes. |
| `schedule_options` | `dict` | `{}` | no | — | — | — | Schedule-specific options: beta {'alpha': 0.6, 'beta': 0.6}, exponential {'sigma_min': 1e-3}, linear_quadratic {'threshold_noise': 0.025, 'linear_steps': <int, default steps // 2>}, or ltx_dynamic {'base_shift': 0.95, 'max_shift': 2.05, 'stretch': True, 'terminal': 0.1} (LTX-2.5 defaults). Ignored when schedule is unset. |
| `manual_sigmas` | `str` | `""` | no | — | — | — | Explicit, comma-separated, descending sigma schedule ('1.0, 0.99375, 0.9875, ..., 0.0'; ComfyUI 'ManualSigmas'-style, e.g. a distilled-LoRA refine tail -- see docs/models/ltx.md). Its length IS the step count: overrides 'steps' and any shift/schedule setting outright. Takes priority over 'schedule'/'schedule_options' when non-empty (the default '' is a no-op, byte-identical to not having this knob). |
| `detail_strength` | `float` | — | no | — | -0.3 | 0.3 | Detail-daemon sigma warp strength (unset = inherit the model's own sampling_settings, or 0 = off if neither sets it; expected range -0.3 to 0.3) |
| `detail_start` | `float` | — | no | — | 0.0 | 1.0 | Detail-daemon warp window start (trajectory fraction; unset = inherit) |
| `detail_end` | `float` | — | no | — | 0.0 | 1.0 | Detail-daemon warp window end (trajectory fraction; unset = inherit) |
| `resolution` | `str` | `"832x480"` | no | — | — | — | Resolution (WxH) |
| `frames` | `int` | `81` | no | — | 1 | 257 | Number of video frames |
| `fps` | `float` | `16.0` | no | — | 1.0 | 60.0 | Output frame rate |
| `quantity` | `int` | `1` | no | — | 1 | 4 | Number of videos |
| `seed` | `int` | `-1` | no | — | -1 | — | Random seed |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `preview` | `bool` | `true` | no | — | — | — | Emit live latent previews (frame 0) to the workbench during sampling |

### <a id="pipe-generator-video-ltx"></a>`generator/video_ltx`

Native LTX-2/2.3 video-director generator (media conditioning, IC-LoRA references, optional audio)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | LTX model bundle |
| `conditioning` | `CONDITIONING` | yes | yes | Encoded prompt conditioning |
| `image` | `IMAGE` | no | yes | Conditioning images (keyframes / first / last) |
| `video` | `VIDEO` | no | yes | Conditioning clips (keyframe clips / IC-LoRA references) |
| `audio` | `AUDIO` | no | yes | User audio track (muxed when audio_source=file) |
| `seed` | `SEED` | no | yes | Random seeds |
| `initial_latent` | `LATENT` | no | yes | Seed latent(s) for a stage-2 refine pass, one per seed. May be combined with image-sourced media_placements (reapplied at this call's own resolution); video-sourced and role='reference' conditioning are rejected alongside it. Absent -> ordinary video/director generation |
| `MODELS` | `SERVICE` | no | no | Model lifecycle service, to release the idle TE's host RAM |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `video` | `VIDEO` | yes | Generated videos (empty when decode=false) |
| `latent` | `LATENT` | yes | Raw per-seed latents (only populated when decode=false) |
| `audio` | `AUDIO` | yes | Decoded/passthrough audio track, one per seed (populated whenever audio=true, independent of decode) -- wire into a stage-2 refine's own 'audio' input with audio_source='passthrough' to mux without regenerating |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `mode` | `str` | `"video"` | yes | `video` | — | — | Generation mode |
| `decode` | `bool` | `true` | no | — | — | — | Decode to video; set false to emit the raw latent for a downstream latent_upscaler/refine stage instead |
| `refine_sigmas` | `str` | `""` | no | — | — | — | Explicit sigma schedule for a refine pass, comma-separated, descending, used VERBATIM (unlike manual_sigmas, the head is not forced to 1.0) -- required when 'initial_latent' is connected |
| `stage2_loras` | `list` | `[]` | no | — | — | — | LoRAs applied ONLY around this call's sampling, ADDED on top of the generation LoRA stack already baked/resident on the shared DiT (e.g. the distilled LoRA for a two-stage refine). Only meaningful when 'initial_latent' is connected (a stage-2 refine call) -- ignored with a warning otherwise |
| `steps` | `int` | `24` | no | — | 1 | 100 | Denoising steps |
| `cfg` | `float` | `4.0` | no | — | 1.0 | 20.0 | True CFG scale |
| `sampler` | `str` | `"euler"` | no | `euler`, `euler_ancestral`, `euler_ancestral_cfg_pp`, `euler_cfg_pp` | — | — | Sampler (conditioned runs support only single-step, no-history samplers -- see module docstring). Euler CFG++ (deterministic) pairs with the Manual Sigmas field on the Advanced tab to reproduce Lightricks' own first-party distilled-refine recipe (distilled LoRA ~0.5 strength, CFG 1.0, no ancestral noise). Euler Ancestral CFG++ is a community (ComfyUI-workflow) variant of the same recipe that adds ancestral noise injection on top. 'euler_ancestral' is LTX-2.5's own stage-1 sampler (stochastic, eta=1.0 by default) -- pair it with schedule='ltx_dynamic' for the matching resolution-aware sigma shift. |
| `resolution` | `str` | `"768x512"` | no | — | — | — | Resolution (WxH) |
| `frames` | `int` | `49` | no | — | 1 | 1001 | Number of video frames (must be 1 + 8*k; up to ~40s at 25fps) |
| `fps` | `float` | `25.0` | no | — | 1.0 | 60.0 | Output frame rate |
| `quantity` | `int` | `1` | no | — | 1 | 4 | Number of videos |
| `seed` | `int` | `-1` | no | — | -1 | — | Random seed |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `preview` | `bool` | `true` | no | — | — | — | Emit live latent previews during sampling |
| `media_placements` | `list` | `[]` | no | — | — | — | Media conditioning placements: [{source: image\|video, index, frame: int\|first\|last, strength, role: keyframe\|reference}]. Empty with image inputs present = image[0] as first frame (i2v) and image[1] (if any) as last frame (FLF). |
| `audio` | `bool` | `false` | no | — | — | — | Produce an audio track |
| `audio_source` | `str` | `"generate"` | no | `generate`, `file`, `passthrough` | — | — | Audio track source when audio=true: generate (AV DiT samples + decodes it here), file (mux the user-supplied audio input), or passthrough (mux an already-finished track from this pipe's own 'audio' output on a prior stage -- e.g. a stage-2 refine muxing stage-1's decoded audio; requires 'initial_latent' to be connected, no re-decode/re-generation) |
| `cfg_zero_star` | `bool` | `true` | no | — | — | — | CFG-Zero*: rescale the uncond branch onto cond before extrapolation (free quality correction) |
| `zero_init_steps` | `int` | `0` | no | — | 0 | 100 | CFG-Zero*: return a zero velocity prediction for the first N steps |
| `nag_scale` | `float` | `1.0` | no | — | 1.0 | 20.0 | Normalized Attention Guidance scale (1.0 = off). Injects the negative prompt into cross-attention so it's enforced even at cfg=1.0 (single-forward speed) — set cfg to 1.0 and nag_scale to ~1.1-1.5 for the speed win; NAG stacks with true CFG if both are on. |
| `nag_tau` | `float` | `3.5` | no | — | 0.1 | 20.0 | NAG norm-clamp threshold (paper default 3.5) |
| `nag_alpha` | `float` | `0.5` | no | — | 0.0 | 1.0 | NAG blend-back-toward-positive weight (paper default 0.5) |
| `apg_eta` | `float` | — | no | — | 0.0 | 1.0 | APG: down-weight of the CFG delta's component parallel to the cond prediction (unset = inherit the model's own sampling_settings, or 1.0 = off / plain CFG if neither sets it; paper explores ~0.0-0.5 to reduce oversaturation at high CFG) |
| `apg_norm_threshold` | `float` | — | no | — | 0.0 | — | APG: cap the guidance delta's norm to this radius (unset = inherit; 0 = off) |
| `apg_momentum` | `float` | — | no | — | -1.0 | 1.0 | APG: reverse-momentum coefficient applied to the guidance delta across steps (unset = inherit; 0 = off; paper uses small negative values, e.g. -0.5) |
| `sampler_options` | `dict` | `{}` | no | — | — | — | Opaque per-sampler options dict forwarded to the chosen sampler, e.g. {'eta': 0.5} for sampler=euler_sde or {'restart_count': 2, 'restart_strength': 0.3} for sampler=euler_restart. Empty/absent = sampler defaults, no effect on euler/dpmpp_2m/unipc. |
| `step_cache` | `dict` | `{}` | no | — | — | — | FBCache step-skipping options: {'rel_threshold': 0.12, 'warmup_steps': 4, 'max_consecutive_skips': 3}. rel_threshold<=0 (default/absent) is off. Only arch forwards that honor the step_cache kwarg actually skip steps; others ignore it harmlessly. Takes priority over the flat step_cache_threshold/step_cache_warmup_steps/step_cache_max_skips knobs below when non-empty (back-compat with existing presets/callers). |
| `step_cache_threshold` | `float` | `0.0` | no | — | 0.0 | 1.0 | FBCache step-skipping: relative-error threshold below which a step's residual is reused instead of recomputed. 0.0 (default) is off; ~0.08 is a conservative starting point, ~0.15 more aggressive. Only pays off on runs of 15+ steps -- warmup_steps alone already covers shorter runs. Ignored when the 'step_cache' dict above is also set. |
| `step_cache_warmup_steps` | `int` | `4` | no | — | 0 | — | FBCache step-skipping: number of leading steps forced to fully compute (never skipped) before the cache is allowed to kick in. Ignored when the 'step_cache' dict above is also set. |
| `step_cache_max_skips` | `int` | `3` | no | — | 0 | — | FBCache step-skipping: maximum number of consecutive steps the cache may skip before a forced recompute. Ignored when the 'step_cache' dict above is also set. |
| `schedule` | `str` | `""` | no | — | — | — | Sigma schedule key: '' (default) or 'shift' for the model's own shift-based ramp, or any key registered on the schedule registry -- 'beta', 'exponential', 'linear_quadratic', 'manual', 'ltx_dynamic' in core, plus whatever a plugin's 'schedules:' manifest root adds. GET /api/sampling/catalog lists what this instance accepts. Prefer 'manual_sigmas' below over setting this to 'manual' directly -- it also fills schedule_options for you. 'ltx_dynamic' only functions on a pipe that feeds image_seq_len into the schedule builder -- currently the LTX generator pipes. |
| `schedule_options` | `dict` | `{}` | no | — | — | — | Schedule-specific options: beta {'alpha': 0.6, 'beta': 0.6}, exponential {'sigma_min': 1e-3}, linear_quadratic {'threshold_noise': 0.025, 'linear_steps': <int, default steps // 2>}, or ltx_dynamic {'base_shift': 0.95, 'max_shift': 2.05, 'stretch': True, 'terminal': 0.1} (LTX-2.5 defaults). Ignored when schedule is unset. |
| `manual_sigmas` | `str` | `""` | no | — | — | — | Explicit, comma-separated, descending sigma schedule ('1.0, 0.99375, 0.9875, ..., 0.0'; ComfyUI 'ManualSigmas'-style, e.g. a distilled-LoRA refine tail -- see docs/models/ltx.md). Its length IS the step count: overrides 'steps' and any shift/schedule setting outright. Takes priority over 'schedule'/'schedule_options' when non-empty (the default '' is a no-op, byte-identical to not having this knob). |
| `detail_strength` | `float` | — | no | — | -0.3 | 0.3 | Detail-daemon sigma warp strength (unset = inherit the model's own sampling_settings, or 0 = off if neither sets it; expected range -0.3 to 0.3) |
| `detail_start` | `float` | — | no | — | 0.0 | 1.0 | Detail-daemon warp window start (trajectory fraction; unset = inherit) |
| `detail_end` | `float` | — | no | — | 0.0 | 1.0 | Detail-daemon warp window end (trajectory fraction; unset = inherit) |
| `quality_mode` | `bool` | `false` | no | — | — | — | Quality recipe (full model, MultiModalGuider): overrides CFG/sampler with the Lightricks first-party single-pass recipe (cfg 3.0, stg 1.0, rescale 0.7, modality 3.0, 30 steps, euler). Mutually exclusive with Distilled recipe. |
| `quality_cfg` | `float` | — | no | — | 1.0 | 20.0 | Quality mode: CFG scale for the video modality (default 3.0) |
| `quality_stg` | `float` | — | no | — | 0.0 | 10.0 | Quality mode: STG (spatio-temporal guidance) scale for video (default 1.0; 0 = off, skips the perturbed forward) |
| `quality_rescale` | `float` | — | no | — | 0.0 | 1.0 | Quality mode: std-preserving rescale blend (default 0.7; 0 = off) |
| `quality_modality` | `float` | — | no | — | 1.0 | 20.0 | Quality mode: cross-modal guidance scale (default 3.0; 1.0 = off, skips the modality-off forward -- only active with an audio track) |
| `quality_stg_blocks` | `str` | — | no | — | — | — | Quality mode: comma-separated transformer block indices for STG perturbation (default '28' for LTX-2.3) |
| `quality_distilled_strength` | `float` | — | no | — | 0.0 | 2.0 | Quality mode with distilled LoRA: LoRA strength override (ComfyUI F-pass uses 0.2; default 0.2 when quality_mode is on and a LoRA is loaded). Not applied automatically -- just an ai_hint. |

### <a id="pipe-generator-video-minimax-h3"></a>`generator/video_minimax_h3`

Native MiniMax-H3 t2va/fl2va video+audio generator

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | MiniMax-H3 model bundle |
| `conditioning` | `CONDITIONING` | yes | yes | Encoded prompt conditioning (context + token_tags -- see model_loader/minimax_h3/clip.py) |
| `image` | `IMAGE` | no | yes | fl2va keyframe image(s), positional with 'keyframe_anchors' |
| `reference_images` | `IMAGE` | no | yes | ref2va reference image(s), first in packed order -- mutually exclusive with 'image'/'keyframe_anchors' (fl2va) |
| `reference_videos` | `VIDEO` | no | yes | ref2va reference video(s), packed after every image reference. Each contributes one visual condition latent (a frame stack, so 5*n+2 latent frames rather than 1) at the canvas its own aspect ratio resolves to |
| `reference_audios` | `AUDIO` | no | yes | ref2va reference audio track(s), packed after every video reference. Each contributes CLEAN audio condition rows and no visual latent at all, and cannot be the only reference kind on a request |
| `director_image` | `IMAGE` | no | yes | Video Director images, indexed by the document's own `media_placements`. Kept apart from 'image' on purpose: these condition latent frames only and never reach the text encoder's vision tower |
| `initial_latent` | `LATENT` | no | yes | Seed latent for a refine pass, one per seed: a raw MiniMax-H3 VIDEO latent (B, 24, T_lat, H_lat, W_lat) in the video VAE's own native (un-normalized) space -- this pipe normalizes it itself, the exact inverse of the '* latents_std + latents_mean' step 'decode' applies. Connected -> 'resolution'/'frames' config are ignored and DERIVED from the latent's own shape instead (H_lat/W_lat must be even -- pixel = latent * 16 has to land on the 32px canvas grid), and 'denoise' controls how much of the schedule is walked. NOT bound by the release canvas's own area cap/short edge -- a refine latent already exists at whatever resolution its own upstream generation used (e.g. 1920x1088, well above that cap); only an aspect-ratio sanity check still applies. Absent -> ordinary generation from pure noise, unchanged. Mutually exclusive with 'document', 'image'/'keyframe_anchors' and every reference input |
| `source_frame_count` | `INT` | no | no | Original (pre-temporal-padding) frame count of a refine's source clip, from the upstream latent_upscaler's own 'source_frame_count' output -- used to trim the padded duplicate tail frames from this pipe's decoded output before mux. Absent -> no trim |
| `audio` | `AUDIO` | no | yes | User audio track (audio_source=file/passthrough) |
| `seed` | `SEED` | no | yes | Random seeds |
| `MODELS` | `SERVICE` | no | no | Model lifecycle service, to release the idle TE's host RAM |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `video` | `VIDEO` | yes | Generated videos (empty when decode=false) |
| `latent` | `LATENT` | yes | Raw per-seed video latents (only populated when decode=false) |
| `audio` | `AUDIO` | yes | Decoded/passthrough audio track, one per seed |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `mode` | `str` | `"video"` | yes | `video`, `references` | — | — | Generation mode. 'video' is t2va/fl2va: the request is the prompt plus any keyframes, and a request with neither is a valid text-only one. 'references' is ref2va: the request MUST carry at least one reference, and a run that resolved to none is refused rather than quietly falling back to t2va -- on a ref2va checkpoint that fallback returns plausible video that ignores the whole point of the request |
| `steps` | `int` | `24` | no | — | 2 | 100 | Denoising steps (video and audio share the count, own schedules) |
| `resolution` | `str` | `"1344x768"` | no | — | — | — | Resolution (WxH); snapped to the 32px canvas grid and the [1:4, 4:1] aspect range |
| `frames` | `int` | `124` | no | — | 1 | 360 | Number of frames at the fixed 24 fps (up to 15s; snapped up to the next 17*n+5 the video VAE can decode -- no enforced floor below that lattice's own minimum, 5 frames). The model's release notes suggest staying near 5-15s, but that is a recommendation, not a limit this pipe enforces |
| `quantity` | `int` | `1` | no | — | 1 | 4 | Number of videos |
| `seed` | `int` | `-1` | no | — | -1 | — | Random seed |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `preview` | `bool` | `true` | no | — | — | — | Emit live latent previews during sampling |
| `keyframe_anchors` | `list` | `[]` | no | — | — | — | Which end of the video each 'image' input anchors, positional with it: ['first'], ['last'] or ['first','last'] (fl2va). Empty with images present = image[0] as first frame, image[1] (if any) as last frame; empty with no images = t2va |
| `audio_source` | `str` | `"generate"` | no | `generate`, `file`, `passthrough` | — | — | Audio track source: generate (decode the jointly-sampled audio latents -- MiniMax-H3 always samples an audio stream, this only picks what the FINAL muxed track is), file (mux the user-supplied audio input instead), or passthrough (mux an already-decoded track from this pipe's own prior 'audio' output, e.g. a future refine stage) |
| `decode` | `bool` | `true` | no | — | — | — | Decode to video; set false to emit the raw latent instead (audio is still decoded/populated either way) |
| `decode_tile_px` | `int` | `256` | no | `0`, `256`, `512` | — | — | Spatial tile size the video VAE decodes in. 256 is the reference implementation's own tile and the only value whose frames match a reference decode exactly; 512 blends at half as many seams and 0 decodes each temporal chunk whole, both of which cost more VRAM per forward. How many tiles run as ONE batched forward is not this knob: that is sized to the free VRAM at decode time. |
| `step_cache_threshold` | `float` | `0.0` | no | — | 0.0 | 1.0 | FBCache step-skipping: relative-change threshold below which the whole transformer forward is skipped and the previous step's output reused. 0.0 (default) is off; ~0.08 is a conservative starting point, ~0.15 more aggressive. Only pays off on runs of 15+ steps. |
| `step_cache_warmup_steps` | `int` | `4` | no | — | 0 | — | FBCache step-skipping: number of leading steps forced to fully compute (never skipped) before the cache is allowed to kick in. |
| `step_cache_max_skips` | `int` | `3` | no | — | 0 | — | FBCache step-skipping: maximum number of consecutive steps the cache may skip before a forced recompute. |
| `sparse_attn` | `str` | `"off"` | no | `off`, `sol`, `sla` | — | — | Sparse attention method: 'off' (default), 'sol' (Sol-Attn, threshold routing) or 'sla' (SLA, top-k routing, pairs with the SLA turbo LoRA). A speed-for-fidelity trade on long sequences, not a free win. Needs a bfloat16/float16 CUDA device with compute capability 8.0+; anywhere else it logs one warning and the generation continues on the normal attention path. |
| `sol_attn_tau` | `float` | `1.0` | no | — | 0.0 | 3.0 | Sol-Attn sparsity temperature: higher skips more KV blocks (faster, less faithful). Only has an effect when 'sparse_attn' is 'sol'. |
| `sla_sparsity` | `float` | `0.9` | no | — | 0.0 | 0.95 | SLA: fraction of key blocks skipped by top-k routing (faster, less faithful the higher it goes). Only has an effect when 'sparse_attn' is 'sla'. |
| `sla_block_size` | `int` | `64` | no | `64`, `128` | — | — | SLA: KV block width, 64 or 128. Only has an effect when 'sparse_attn' is 'sla'. |
| `sparse_attn_dense_last_steps` | `int` | `2` | no | — | 0 | 10 | Sparse attention: number of trailing steps of each window run on the normal dense attention path. The end of a trajectory carries the least noise, so a sparse approximation there is the most visible. At or above the step count the feature is effectively off. |
| `seq_chunk_rows` | `int` | `0` | no | — | 0 | — | Low-VRAM sequence chunking: row-chunks the DiT's per-block qkv projection and SwiGLU MLP over the packed sequence so their intermediate transients (fused qkv, 2x ffn_dim) never exist full-length at once -- the attention core itself is unaffected. 0 (default) is off and byte-identical to a run without the feature; a value below the packed sequence length trades a small perf cost (quantized weights are re-dequantized per chunk) for materially lower peak VRAM. Only worth setting on very long sequences (e.g. a high-resolution refine); leave off for an ordinary generation. |
| `sampler` | `str` | `"euler"` | no | `euler`, `res_multistep`, `dpmpp_2m`, `er_sde`, `sa_solver` | — | — | Which solver advances each stream between two grid sigmas. 'euler' is the reference first-order step. 'res_multistep' and 'dpmpp_2m' are second-order multistep solvers -- they reuse the previous step's x0 estimate, so they cost no extra model evaluations and mostly pay off at low step counts. 'sa_solver' is a deterministic order-2 predictor-corrector, another no-extra-cost option in the same family. 'er_sde' is stochastic (adds noise every step, seeded like the rest of the request) -- more diverse, less reproducible frame-to-frame than the deterministic solvers. Both streams run the chosen solver on their OWN sigma grid, and the history is per stream and per Director window. |
| `scheduler` | `str` | `"simple"` | no | `simple`, `beta` | — | — | Where the schedule places its knots. 'simple' is the reference uniform grid; 'beta' pushes it through a Beta(0.6, 0.6) quantile function, which clusters knots at both ends of the trajectory. The video (shift 12) and audio (shift 3) schedules are both derived from the SAME knots, so the two streams stay paired whichever scheduler runs. Cannot be combined with a manual sigma grid. |
| `manual_sigmas` | `str` | `""` | no | — | — | — | Explicit video sigma grid, comma-separated and strictly descending, ending at exactly 0.0 (e.g. '1.0, 0.86, 0.5, 0.0'). Empty (default) computes the grid from 'steps' at shift 12.0. The list's length is the grid, so it drives one fewer model evaluation than it has values, and it OVERRIDES 'steps'. |
| `manual_audio_sigmas` | `str` | `""` | no | — | — | — | Explicit audio sigma grid, same format as 'manual_sigmas'. Empty (default) computes the grid at shift 3.0, sized to match whatever the video stream ended up with. Set both and the two lists must have the same length. |
| `denoise` | `float` | `1.0` | no | — | 0.01 | 1.0 | Refine strength for a run seeded from the 'initial_latent' input: the fraction of the full schedule actually walked. 1.0 (default) is a full generation from pure noise, byte-identical to what 'steps' alone would produce. Below 1.0, ComfyUI's BasicScheduler convention: 'steps' knots are kept off the TAIL of a longer ceil(steps/denoise)-step schedule, so the run starts short of full noise instead of at it -- a detailer-style second pass (e.g. 4 steps, denoise 0.45). Only meaningful with 'initial_latent' connected; set otherwise is refused. |
| `video_sigma_shift` | `float` | `12.0` | no | — | 0.1 | 50.0 | Video stream's exponential-shift constant (default 12, the released checkpoint's own -- see 'manual_sigmas'). A refine pass over an already-upsampled latent typically runs at a lower shift (e.g. 9) than a fresh generation; the audio stream's own shift (3.0) is a separate, unaffected knob. |
| `document` | `dict` | — | no | — | — | — | A normalized Video Director document. With a routed multi-segment `director` document this pipe runs a SLIDING WINDOW: one full MiniMax-H3 generation per segment, each continuation window pinned to its predecessor's final latent frames, trimmed back to the overlap and stitched into one clip. Anything else (absent, or a single-shot t2v/flf document) leaves the pipe on its ordinary one-generation path, where `frames`/`resolution`/`steps` above are the request. |
| `references` | `list` | `[]` | no | — | — | — | The ref2va references form field's own value, passed through untouched (a list of `{path, relative_path?, label?, ...}` dicts, one per entry, in the SAME order as the 'reference_images' input's loaded array -- same idiom as 'document' above. Used ONLY to cross-validate that the loaded 'reference_images' array actually has as many entries as the preset's references field declared, the same guard 'document'/'director_image' get for Video Director; nothing here reaches the layout or the presentation, both of which read the LOADED images' own order. A per-item 'label' is a display/LLM handle (reaches the model via the chat context, not this pipe) -- never the `<Picture N>` text, which is always pipe-computed from position (see 'reference_images' below). |
| `reference_videos` | `list` | `[]` | no | — | — | — | The ref2va reference-VIDEOS form field's own value, passed through untouched -- the video counterpart of 'references' above, and used for the same count cross-check against the 'reference_videos' input. |
| `reference_audios` | `list` | `[]` | no | — | — | — | The ref2va reference-AUDIO form field's own value, passed through untouched -- the audio counterpart of 'references' above, and used for the same count cross-check against the 'reference_audios' input. |

### <a id="pipe-generator-z-image"></a>`generator/z_image`

Native Z-Image generator (flow matching, true CFG)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | Z-Image model bundle |
| `conditioning` | `CONDITIONING` | yes | yes | Encoded prompt conditioning (per image) |
| `seed` | `SEED` | no | yes | Random seeds |
| `image` | `IMAGE` | no | yes | Input images for img2img mode |
| `MODELS` | `SERVICE` | no | no | Model lifecycle service, to release the idle TE's VRAM before sampling |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Generated images |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `mode` | `str` | `"txt2img"` | yes | `txt2img`, `img2img` | — | — | Generation mode |
| `denoise` | `float` | `0.55` | no | — | 0.0 | 1.0 | img2img denoise strength (fraction of the schedule to walk) |
| `img2img_scale` | `float` | `0.0` | no | — | 0.0 | 8.0 | Literal multiplier of the SOURCE image's own (width, height) for the img2img target size, snapped to the model's granularity same as every other path here. 0 (default) is off and leaves the existing resolution-area-match / area-cap behaviour byte-identical -- set this instead of (not alongside) the mode's ``resolution`` config for a true "upscale by Nx of whatever was uploaded" mode. |
| `steps` | `int` | `8` | no | — | 1 | 100 | Denoising steps (turbo ~8, base ~30) |
| `guidance` | `float` | `1.0` | no | — | 0.0 | 30.0 | True CFG scale (turbo 1.0 = single forward; base ~4) |
| `shift` | `float` | — | no | — | — | — | Sigma-shift override; blank -> spec default (3.0) |
| `sampler` | `str` | `"euler"` | no | `euler`, `dpmpp_2m`, `dpmpp_2m_sde`, `dpmpp_3m`, `res_multistep`, `unipc`, `lcm` | — | — | Sampler |
| `resolution` | `str` | `"1024x1024"` | no | — | — | — | Resolution (WxH) |
| `quantity` | `int` | `1` | no | — | 1 | 10 | Number of images |
| `seed` | `int` | `-1` | no | — | -1 | — | Random seed |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `preview` | `bool` | `true` | no | — | — | — | Emit live latent previews to the workbench during sampling |
| `step_cache` | `dict` | `{}` | no | — | — | — | FBCache step-skipping options, forwarded to NativeGenerator.sample() unmodified: {'rel_threshold': 0.12, 'warmup_steps': 4, 'max_consecutive_skips': 3}. rel_threshold<=0 (default/absent) is off and never wraps the guidance strategy -- byte-identical to leaving this unset. Read directly by FlowMatchGeneratorPipe.build_context, not through the flat step_cache_threshold/warmup_steps/max_skips resolver the Wan/LTX video pipes use (this family has no such resolver). |
| `spectral_progressive` | `dict` | — | no | — | — | — | Spectral Progressive Diffusion (opt-in prototype): denoise the early, high-sigma steps at a reduced latent resolution and grow to full resolution as the schedule's frequency bands stop being noise-dominated. {'scales': [0.5, 1.0], 'delta': 0.01, 'power_beta': 2.5, 'power_amplitude': 1.0, 'basis': 'fft'\|'dct', 'transitions': null, 'enabled': true} -- usually only 'scales' is worth setting, the rest derive a sensible schedule from it. Off by default (needs GPU validation). Only engages on a constant-shift, 4D-image, txt2img family (Flux2/Klein, Z-Image); every dynamic-mu family (Flux1, Krea-2), video family, or img2img run silently falls back to the normal path. |

## inpaint_region_crop

### <a id="pipe-inpaint-region-crop"></a>`inpaint_region_crop`

Crop image to masked region for memory-efficient inpainting (Fooocus-style)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `image` | `IMAGE` | yes | no | Source image for inpainting |
| `mask` | `IMAGE` | yes | no | Preprocessed mask |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | no | Cropped image (masked region only) |
| `mask` | `IMAGE` | no | Cropped mask |
| `crop_region` | `DICT` | no | Crop region metadata for restore |
| `original_image` | `IMAGE` | no | Original full image for restore |
| `original_mask` | `IMAGE` | no | Original full-size mask for color correction |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `k` | `float` | `0.618` | no | — | — | — | Padding factor (0.618 = Fooocus default) |
| `min_size` | `int` | `1024` | no | — | — | — | Minimum size to upscale to |
| `max_size` | `int` | `1024` | no | — | — | — | Maximum size to downscale to |

## inpaint_region_restore

### <a id="pipe-inpaint-region-restore"></a>`inpaint_region_restore`

Restore inpainted region back into original full image (Fooocus-style)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `inpainted_image` | `IMAGE` | yes | no | Inpainted cropped image |
| `crop_region` | `DICT` | yes | no | Crop region metadata from crop pipe |
| `original_image` | `IMAGE` | yes | no | Original full image |
| `original_mask` | `IMAGE` | no | no | Original full-size mask for color correction (Fooocus-style) |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Full image with inpainted region restored |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `color_correction` | `bool` | `true` | no | — | — | — | Apply color correction for smooth blending |

## interpolator

### <a id="pipe-interpolator-rife"></a>`interpolator/rife`

RIFE 4.x frame interpolation (2x/4x) with audio passthrough

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `video` | `VIDEO` | yes | yes | Source video to interpolate |
| `MODELS` | `SERVICE` | no | no | Model lifecycle service for RIFE checkpoint reuse across clips |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `video` | `VIDEO` | yes | Frame-interpolated video |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `model` | `dict` | — | yes | — | — | — | RIFE checkpoint ({file_path, name}) from the vfi model field |
| `factor` | `int` | `2` | no | `2`, `4` | — | — | Interpolation factor: 2x inserts t=0.5; 4x inserts t=0.25/0.5/0.75 |
| `flow_scale` | `float` | `1.0` | no | `1.0`, `0.5` | — | — | Flow-computation scale (0.5 for high-res >2K inputs) |
| `keep_audio` | `bool` | `true` | no | — | — | — | Mux the source video's audio track into the output |

## latent_upscaler

### <a id="pipe-latent-upscaler-ltx"></a>`latent_upscaler/ltx`

Upsample an LTX-2/2.3/2.5 video latent in latent space -- spatially (x1.5/x2.0) or temporally (x2 frames) -- via the model's optional upscaler checkpoints

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | LTX model bundle (needs the VAE plus the upsampler slot 'mode' selects) |
| `latent` | `LATENT` | no | no | Direct latent to upsample (in-flow two-stage) |
| `video` | `VIDEO` | no | yes | Existing video file to VAE-encode then upsample (standalone upscale mode) |
| `MODELS` | `SERVICE` | no | no | Model lifecycle service, to release the idle TE's host RAM |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `latent` | `LATENT` | no | Upsampled latent |
| `source_frame_count` | `INT` | no | Original (pre-temporal-padding) frame count of the 'video' input, before _pad_frames_to_temporal_grid repeated its last frame up to the VAE's 1+8k lattice -- lets a downstream generator trim that padding back out of its own decoded output before mux. In mode='temporal' this is mapped through the same T -> 2T-1 the upsampler applies, so it still names the source content's extent on the NEW timeline. None on the 'latent'-input path (in-flow two-stage: no video was decoded here, so no padding was ever added) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `mode` | `str` | `"spatial"` | no | `spatial`, `temporal` | — | — | Which upscaler to run: 'spatial' (model_loader/ltx's upscale_model, x1.5/x2.0, frame count unchanged) or 'temporal' (its temporal_upscale_model, LTX-2.5 x2, T -> 2T-1 latent frames) |
| `max_frames` | `int` | `1001` | no | — | 1 | 1001 | Max frames to read from a 'video' input (standalone upscale mode) |

## mask_preprocessor

### <a id="pipe-mask-preprocessor"></a>`mask_preprocessor`

Preprocess inpainting mask with dilation and blur (Fooocus-style)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `mask` | `IMAGE` | yes | no | Raw mask image to preprocess |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `mask` | `IMAGE` | no | Preprocessed mask (dilated and blurred) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `dilate_iterations` | `int` | `4` | no | — | 0 | 20 | Number of dilation iterations to expand mask |
| `blur_radius` | `int` | `4` | no | — | 0 | 20 | Gaussian blur radius for smooth mask edges |

## matting

### <a id="pipe-matting-birefnet"></a>`matting/birefnet`

Background removal via BiRefNet, producing an RGBA cutout

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `image` | `IMAGE` | yes | yes | Source image(s) to cut the subject out of |
| `MODELS` | `SERVICE` | no | no | Model lifecycle service for checkpoint caching |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | RGBA cutout(s), alpha = matted subject |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `model` | `dict` | — | yes | — | — | — | BiRefNet matting checkpoint ({file_path, name}) from the model picker |
| `matte_strength` | `int` | `50` | no | — | 0 | 100 | Smoothstep tightening of the raw sigmoid alpha (0 = identity, 100 = hard threshold at 128) |
| `feather` | `float` | `0.0` | no | — | 0.0 | 16.0 | Gaussian-blur radius (px) applied to the alpha edge |

## media_loader

### <a id="pipe-media-loader"></a>`media_loader`

Load media files (images/videos/audio) from form file paths and convert them to proper IOType objects

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `SETTINGS` | `SERVICE` | no | no | Settings manager, to resolve storage-root-relative media paths |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Loaded image files |
| `video` | `VIDEO` | yes | Loaded video files |
| `audio` | `AUDIO` | yes | Loaded audio file paths |
| `media_metadata` | `DICT` | yes | Metadata about loaded media files |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `validate_files` | `bool` | `true` | no | — | — | — | Validate that files exist and are readable |
| `max_file_size` | `int` | `104857600` | no | — | 1024 | 1073741824 | Maximum file size in bytes (default 100MB) |
| `auto_detect_type` | `bool` | `true` | no | — | — | — | Auto-detect media type from file extension |
| `supported_image_formats` | `list` | `[".jpg", ".jpeg", ".png", ".bmp", ".tiff", ".webp"]` | no | — | — | — | Supported image file extensions |
| `supported_video_formats` | `list` | `[".mp4", ".avi", ".mov", ".mkv", ".webm", ".flv", ".wmv"]` | no | — | — | — | Supported video file extensions |
| `supported_audio_formats` | `list` | `[".mp3", ".wav", ".flac", ".ogg", ".m4a", ".aac"]` | no | — | — | — | Supported audio file extensions |
| `media` | `list` | `[]` | no | — | — | — | List of media files to load with type and path |

## model_loader

### <a id="pipe-model-loader-anima"></a>`model_loader/anima`

Load a native Anima checkpoint set (MiniTrainDIT + Qwen3-0.6B TE + causal-3D VAE)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `MODELS` | `SERVICE` | no | no | Model lifecycle service for per-component reuse |
| `GPU` | `SERVICE` | no | no | GPU manager for the VRAM budget |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `model` | `MODEL` | no | Anima model bundle (DiT + TE + VAE) |
| `text_encoder` | `TEXT_ENCODER` | no | Qwen3-0.6B text encoder (ClipTextEncoder ABC) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `diffusion_model` | `dict` | — | yes | — | — | — | Anima MiniTrainDIT checkpoint |
| `text_encoder` | `dict` | — | yes | — | — | — | Qwen3-0.6B text encoder (single TE, no CLIP-L) |
| `vae` | `dict` | — | yes | — | — | — | Anima (Wan-2.1 causal-3D) VAE |
| `loras` | `list` | `[]` | no | — | — | — | LoRA adapters (busts only the DiT cache) |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `dtype` | `str` | `"bfloat16"` | no | `bfloat16`, `float16`, `float32` | — | — | Compute dtype |
| `vram_limit_gb` | `float` | — | no | — | — | — | VRAM budget hint (backend-injected) |

### <a id="pipe-model-loader-flux"></a>`model_loader/flux`

Load a native Flux-family checkpoint set (DiT + text encoder + VAE)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `MODELS` | `SERVICE` | no | no | Model lifecycle service for per-component reuse |
| `GPU` | `SERVICE` | no | no | GPU manager for the VRAM budget |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `model` | `MODEL` | no | Flux model bundle (DiT + TE + VAE) |
| `text_encoder` | `TEXT_ENCODER` | no | Flux text encoder (ClipTextEncoder ABC) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `diffusion_model` | `dict` | — | yes | — | — | — | Flux DiT checkpoint |
| `text_encoder` | `dict` | — | yes | — | — | — | Text encoder (T5-XXL for Flux1, Qwen3 for Klein) |
| `clip_l` | `dict` | — | no | — | — | — | CLIP-L (Flux1 only; omit for Klein/Flux2) |
| `vae` | `dict` | — | yes | — | — | — | Flux VAE |
| `loras` | `list` | `[]` | no | — | — | — | LoRA adapters (patched in place on an already-cached DiT) |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `dtype` | `str` | `"bfloat16"` | no | `bfloat16`, `float16`, `float32` | — | — | Compute dtype |
| `vram_limit_gb` | `float` | — | no | — | — | — | VRAM budget hint (backend-injected) |

### <a id="pipe-model-loader-krea2"></a>`model_loader/krea2`

Load a native Krea-2 checkpoint set (DiT + Qwen3-VL TE + Qwen-Image VAE)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `MODELS` | `SERVICE` | no | no | Model lifecycle service for per-component reuse |
| `GPU` | `SERVICE` | no | no | GPU manager for the VRAM budget |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `model` | `MODEL` | no | Krea-2 model bundle (DiT + TE + VAE) |
| `text_encoder` | `TEXT_ENCODER` | no | Krea-2 text encoder (ClipTextEncoder ABC) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `diffusion_model` | `dict` | — | yes | — | — | — | Krea-2 DiT checkpoint (bf16) |
| `text_encoder` | `dict` | — | yes | — | — | — | Qwen3-VL-4B text encoder |
| `vae` | `dict` | — | yes | — | — | — | Qwen-Image causal-3D VAE |
| `loras` | `list` | `[]` | no | — | — | — | LoRA adapters (patched in place on an already-cached DiT). An entry may add 'step_start'/'step_end' (1-based, inclusive) to be active only inside that step range — such an entry is NOT baked into the model; the generator's sampling loop switches it on at the window's first step and off after its last. Omit both keys for the ordinary always-on behaviour. |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `dtype` | `str` | `"bfloat16"` | no | `bfloat16`, `float16`, `float32` | — | — | Compute dtype |
| `vram_limit_gb` | `float` | — | no | — | — | — | VRAM budget hint (backend-injected) |
| `vision` | `bool` | `false` | no | — | — | — | Load the Qwen3-VL vision tower for image-grounded instruction encoding (Krea-2 edit mode) |

### <a id="pipe-model-loader-ltx"></a>`model_loader/ltx`

Load a native LTX-2/2.3/2.5 checkpoint set (all-in-one or split DiT + TE + video VAE)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `MODELS` | `SERVICE` | no | no | Model lifecycle service for per-component reuse |
| `GPU` | `SERVICE` | no | no | GPU manager for the VRAM budget |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `model` | `MODEL` | no | LTX model bundle (DiT + TE + VAE + projections) |
| `text_encoder` | `TEXT_ENCODER` | no | LTX Gemma3 text encoder (ClipTextEncoder ABC) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `model` | `dict` | — | yes | — | — | — | LTX DiT checkpoint (all-in-one for 2.0/2.3, transformer-only for 2.5's split layout) |
| `text_encoder` | `dict` | — | yes | — | — | — | Text encoder (Gemma3-12B for 2.0/2.3, Gemma4-12B-with-proj for 2.5) |
| `vae` | `dict` | — | no | — | — | — | Optional VAE override; defaults to the video VAE embedded in the all-in-one checkpoint (required for LTX-2.5 split checkpoints) |
| `audio` | `bool` | `false` | no | — | — | — | Load the audio VAE + vocoder for audio-video generation |
| `audio_model` | `dict` | — | no | — | — | — | Optional separate audio-VAE + vocoder checkpoint (LTX-2.5 split layout); defaults to the audio VAE/vocoder embedded in the all-in-one `model` checkpoint |
| `upscale_model` | `dict` | — | no | — | — | — | Optional LTX-2.3/2.5 spatial latent-upscaler checkpoint |
| `temporal_upscale_model` | `dict` | — | no | — | — | — | Optional LTX-2.5 temporal x2 latent-upscaler checkpoint |
| `duration_head` | `dict` | — | no | — | — | — | Optional LTX-2.5 duration head (predicts a shot's natural length from the prompt) |
| `loras` | `list` | `[]` | no | — | — | — | DiT LoRAs |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `dtype` | `str` | `"bfloat16"` | no | `bfloat16`, `float16`, `float32` | — | — | Compute dtype |
| `vram_limit_gb` | `float` | — | no | — | — | — | VRAM budget hint (backend-injected) |

### <a id="pipe-model-loader-maya"></a>`model_loader/maya`

Load Maya text-to-speech model components (Model, Tokenizer, SNAC Codec)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `MODELS` | `SERVICE` | no | no | Model lifecycle service for cross-generation model reuse |
| `ASSETS` | `SERVICE` | no | no | Asset fetcher, to mirror the model and codec repos into the depot |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `model` | `MODEL` | no | Loaded Maya model with all components |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `model_id` | `str` | `"maya-research/maya1"` | yes | — | — | — | Maya model path or HF model ID |
| `snac_model` | `str` | `"hubertsiuzdak/snac_24khz"` | yes | — | — | — | SNAC codec model ID |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Device to load model on |
| `dtype` | `str` | `"bfloat16"` | no | `bfloat16`, `float16`, `float32` | — | — | Data type for model weights |

### <a id="pipe-model-loader-minimax-h3"></a>`model_loader/minimax_h3`

Load a native MiniMax-H3 checkpoint set (DiT + Qwen3-VL-32B TE + video VAE + audio VAE)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `MODELS` | `SERVICE` | no | no | Model lifecycle service for per-component reuse |
| `GPU` | `SERVICE` | no | no | GPU manager for the VRAM budget |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `model` | `MODEL` | no | MiniMax-H3 model bundle (DiT + TE + video VAE + audio VAE) |
| `text_encoder` | `TEXT_ENCODER` | no | MiniMax-H3 Qwen3-VL-32B text encoder (ClipTextEncoder ABC) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `model` | `dict` | — | yes | — | — | — | MiniMax-H3 DiT checkpoint (t2va/fl2va) |
| `text_encoder` | `dict` | — | yes | — | — | — | Qwen3-VL-32B text encoder |
| `video_vae` | `dict` | — | yes | — | — | — | MiniMax-H3 video VAE |
| `audio_vae` | `dict` | — | yes | — | — | — | MiniMax-H3 audio VAE (always loaded -- audio is inherent) |
| `loras` | `list` | `[]` | no | — | — | — | DiT LoRAs |
| `vdn_module` | `dict` | — | no | — | — | — | Video-DeltaNet linear branch to attach to the DiT (turbo hybrid attention) |
| `dense_time_embedder` | `dict` | — | no | — | — | — | Full-checkpoint time_embedder tensors, needed only to translate an adapter's AdaLN rows onto a pruned DiT |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `dtype` | `str` | `"bfloat16"` | no | `bfloat16`, `float16`, `float32` | — | — | Compute dtype |
| `vram_limit_gb` | `float` | — | no | — | — | — | VRAM budget hint (backend-injected) |

### <a id="pipe-model-loader-minimax-music3"></a>`model_loader/minimax_music3`

Load a native MiniMax-Music3 checkpoint set (DiT + fused text encoder + DAV vocoder)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `MODELS` | `SERVICE` | no | no | Model lifecycle service for per-component reuse |
| `GPU` | `SERVICE` | no | no | GPU manager for the VRAM budget |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `model` | `MODEL` | no | MiniMax-Music3 model bundle (DiT + text encoder + DAV) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `model` | `dict` | — | yes | — | — | — | MiniMax-Music3 flow-matching DiT checkpoint |
| `text_encoder` | `dict` | — | yes | — | — | — | MiniMax-Music3 fused text encoder (global LLM + depth decoder + tokenizer) |
| `vae` | `dict` | — | yes | — | — | — | MiniMax-Music3 DAV vocoder |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `dtype` | `str` | `"bfloat16"` | no | `bfloat16`, `float16`, `float32` | — | — | Compute dtype |
| `vram_limit_gb` | `float` | — | no | — | — | — | VRAM budget hint (backend-injected) |

### <a id="pipe-model-loader-qwen"></a>`model_loader/qwen`

Load a native Qwen-Image checkpoint set (MMDiT + Qwen2.5-VL TE + causal-3D VAE)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `MODELS` | `SERVICE` | no | no | Model lifecycle service for per-component reuse |
| `GPU` | `SERVICE` | no | no | GPU manager for the VRAM budget |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `model` | `MODEL` | no | Qwen-Image model bundle (DiT + TE + VAE) |
| `text_encoder` | `TEXT_ENCODER` | no | Qwen2.5-VL text encoder (ClipTextEncoder ABC) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `diffusion_model` | `dict` | — | yes | — | — | — | Qwen-Image MMDiT checkpoint |
| `text_encoder` | `dict` | — | yes | — | — | — | Qwen2.5-VL text encoder (single TE, no CLIP-L) |
| `vae` | `dict` | — | yes | — | — | — | Qwen-Image (Wan-2.1 causal-3D) VAE |
| `loras` | `list` | `[]` | no | — | — | — | LoRA adapters (busts only the DiT cache) |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `dtype` | `str` | `"bfloat16"` | no | `bfloat16`, `float16`, `float32` | — | — | Compute dtype |
| `vram_limit_gb` | `float` | — | no | — | — | — | VRAM budget hint (backend-injected) |
| `vision` | `bool` | `false` | no | — | — | — | Load the vision tower for image-conditioned editing |

### <a id="pipe-model-loader-seedvr2"></a>`model_loader/seedvr2`

Load a native SeedVR2 checkpoint set (NaDiT + self-normalizing causal-video VAE + fixed prompt embedding)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `MODELS` | `SERVICE` | no | no | Model lifecycle service for per-component reuse |
| `GPU` | `SERVICE` | no | no | GPU manager for the VRAM budget |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `model` | `MODEL` | no | SeedVR2 model bundle (DiT + VAE + fixed prompt embedding) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `diffusion_model` | `dict` | — | yes | — | — | — | SeedVR2 NaDiT checkpoint |
| `vae` | `dict` | — | yes | — | — | — | SeedVR2 self-normalizing causal-video VAE |
| `prompt_embedding` | `dict` | — | yes | — | — | — | Fixed positive prompt embedding (.pt tensor); no live text encoder |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `dtype` | `str` | `"bfloat16"` | no | `bfloat16`, `float16`, `float32` | — | — | Compute dtype |
| `vram_limit_gb` | `float` | — | no | — | — | — | VRAM budget hint (backend-injected) |

### <a id="pipe-model-loader-trellis2"></a>`model_loader/trellis2`

Load a native TRELLIS.2 set (4 flow DiTs + 3 decoders + DINOv3 encoder)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `MODELS` | `SERVICE` | no | no | Model lifecycle service for per-component reuse |
| `GPU` | `SERVICE` | no | no | GPU manager for the VRAM budget |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `model` | `MODEL` | no | TRELLIS.2 model bundle |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `diffusion_model` | `dict` | — | yes | — | — | — | TRELLIS.2 transformer bundle (all four flow DiTs) |
| `shape_vae` | `dict` | — | yes | — | — | — | Shape VAE (sparse-structure + FlexiDualGrid decoders) |
| `texture_vae` | `dict` | — | yes | — | — | — | Texture VAE decoder |
| `image_encoder` | `dict` | — | yes | — | — | — | DINOv3 ViT-L/16 image encoder |
| `matting_model` | `dict` | — | no | — | — | — | BiRefNet checkpoint for background removal on an opaque image. Optional — an image that already has a transparent background needs none. |
| `resolution_tier` | `str` | `"1024"` | no | `1024`, `1536`, `512` | — | — | Reconstruction path. 512 is a single shape pass; 1024 and 1536 are cascades and load a second shape flow. |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `dtype` | `str` | `"bfloat16"` | no | `bfloat16`, `float16`, `float32` | — | — | Compute dtype |
| `vram_limit_gb` | `float` | — | no | — | — | — | VRAM budget hint (backend-injected) |

### <a id="pipe-model-loader-wan22"></a>`model_loader/wan22`

Load a native Wan video checkpoint set (high/low DiT + UMT5 + causal-3D VAE)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `MODELS` | `SERVICE` | no | no | Model lifecycle service for per-component reuse |
| `GPU` | `SERVICE` | no | no | GPU manager for the VRAM budget |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `model` | `MODEL` | no | Wan model bundle (DiT(s) + TE + VAE) |
| `text_encoder` | `TEXT_ENCODER` | no | Wan UMT5 text encoder (ClipTextEncoder ABC) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `high_noise_model` | `dict` | — | yes | — | — | — | Wan DiT (high-noise expert, or the only DiT) |
| `low_noise_model` | `dict` | — | no | — | — | — | Wan low-noise expert (2.2 14B dual-expert only) |
| `text_encoder` | `dict` | — | yes | — | — | — | UMT5-XXL text encoder |
| `vae` | `dict` | — | yes | — | — | — | Wan causal-3D VAE |
| `loras_high` | `list` | `[]` | no | — | — | — | LoRAs for the high-noise expert (or the only DiT); busts only that DiT's cache |
| `loras_low` | `list` | `[]` | no | — | — | — | LoRAs for the low-noise expert (Wan 2.2 pairs ship separate high/low files) |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `dtype` | `str` | `"bfloat16"` | no | `bfloat16`, `float16`, `float32` | — | — | Compute dtype |
| `vram_limit_gb` | `float` | — | no | — | — | — | VRAM budget hint (backend-injected) |

### <a id="pipe-model-loader-z-image"></a>`model_loader/z_image`

Load a native Z-Image checkpoint set (NextDiT + Qwen3-4B TE + 2D AE)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `MODELS` | `SERVICE` | no | no | Model lifecycle service for per-component reuse |
| `GPU` | `SERVICE` | no | no | GPU manager for the VRAM budget |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `model` | `MODEL` | no | Z-Image model bundle (DiT + TE + VAE) |
| `text_encoder` | `TEXT_ENCODER` | no | Qwen3-4B text encoder (ClipTextEncoder ABC) |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `diffusion_model` | `dict` | — | yes | — | — | — | Z-Image NextDiT checkpoint |
| `text_encoder` | `dict` | — | yes | — | — | — | Qwen3-4B text encoder (single TE, no CLIP-L) |
| `vae` | `dict` | — | yes | — | — | — | Z-Image (Flux-style 2D) VAE |
| `loras` | `list` | `[]` | no | — | — | — | LoRA adapters (busts only the DiT cache) |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |
| `dtype` | `str` | `"bfloat16"` | no | `bfloat16`, `float16`, `float32` | — | — | Compute dtype |
| `vram_limit_gb` | `float` | — | no | — | — | — | VRAM budget hint (backend-injected) |

## output_skipper

### <a id="pipe-output-skipper"></a>`output_skipper`

Filter and skip specific outputs from the previous pipe by type and index

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `image` | `IMAGE` | no | yes | Input images to filter |
| `video` | `VIDEO` | no | yes | Input videos to filter |
| `mask` | `MASK` | no | yes | Input masks to filter |
| `latent` | `LATENT` | no | yes | Input latents to filter |
| `seed` | `SEED` | no | yes | Input seeds to filter |
| `conditioning` | `CONDITIONING` | no | yes | Input conditioning to filter |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Filtered images |
| `video` | `VIDEO` | yes | Filtered videos |
| `mask` | `MASK` | yes | Filtered masks |
| `latent` | `LATENT` | yes | Filtered latents |
| `seed` | `SEED` | yes | Filtered seeds |
| `conditioning` | `CONDITIONING` | yes | Filtered conditioning |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `rules` | `list` | `[]` | no | — | — | — | List of filtering rules. Each rule should have: output_type, action ('skip'/'keep'), and indices or count |

## param_emitter

### <a id="pipe-param-emitter"></a>`param_emitter`

Pipe that emits generation parameters for tracking and storage

_No inputs._

**Outputs**: none.

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `parameters` | `list` | `[]` | yes | — | — | — | List of [name, value] parameter pairs |
| `quantity` | `int` | `1` | no | — | 1 | 20 | Number of outputs being generated |
| `passes` | `int` | `1` | no | — | 1 | 8 | How many times the batch is saved to the gallery (a refinement pass that saves alongside the base batch is a second pass). One parameter row is written per saved index, i.e. quantity x passes rows. |

## prompt_encoder

### <a id="pipe-prompt-encoder"></a>`prompt_encoder`

Encodes prompts using CLIP text encoders for SDXL

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `text_encoder` | `TEXT_ENCODER` | yes | no | Text encoder for prompt processing |
| `p_prompt` | `TEXT` | no | no | Positive prompt (optional, from prompt_expander) |
| `n_prompt` | `TEXT` | no | no | Negative prompt (optional, from prompt_expander) |
| `MODELS` | `SERVICE` | no | no | Model lifecycle service for cross-generation reuse |
| `image` | `IMAGE` | no | yes | Source image(s) for a vision-conditioned encoder |
| `reference_image` | `IMAGE` | no | yes | ref2va reference image(s), in packed order |
| `reference_video` | `VIDEO` | no | yes | ref2va reference video(s), packed after every image reference |
| `reference_audio` | `AUDIO` | no | yes | ref2va reference audio track(s), packed after every video reference |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `conditioning` | `CONDITIONING` | yes | Encoded prompt conditioning tensors |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `p_prompt` | `dict` | `{}` | no | — | — | — | Positive prompt configuration |
| `n_prompt` | `dict` | `{}` | no | — | — | — | Negative prompt configuration |
| `quantity` | `int` | `1` | no | — | 1 | 20 | Number of images to generate |
| `guidance_scale` | `float` | `7.5` | no | — | 1.0 | 30.0 | Guidance scale for CFG |
| `nag_scale` | `float` | `1.0` | no | — | 1.0 | 20.0 | Normalized Attention Guidance scale, mirrored from the generator's own nag_scale config. NAG needs the negative prompt encoded even at guidance_scale=1.0 (true CFG off), so >1.0 here forces the negative pass on regardless of guidance_scale |
| `clip_skip` | `int` | — | no | — | 1 | 12 | Number of CLIP layers to skip |
| `pairs` | `list` | `[]` | no | — | — | — | Per-image expanded prompt pairs (from the prompt expander) |
| `positive_embeddings` | `list` | `[]` | no | — | — | — | Positive text embeddings configuration |
| `negative_embeddings` | `list` | `[]` | no | — | — | — | Negative text embeddings configuration |
| `prompt_helpers` | `dict` | `{}` | no | — | — | — | Prompt helper data |
| `grounding_px` | `int` | `768` | no | — | 0 | 4096 | Longest-side cap (px) for the image sent to a vision-grounded text encoder; 0 uses native resolution |
| `system_prompt` | `str` | — | no | — | — | — | Override the vision-grounded encoder's default system prompt |
| `image_pixel_budget` | `float` | `0.0` | no | — | 0.0 | 16.0 | Cap the pixel AREA of each image handed to a vision-grounded text encoder, as a multiple of the output canvas area (`output_resolution`); 0 leaves the encoder's own bound in place |
| `output_resolution` | `str` | — | no | — | — | — | Output canvas as WxH — the area `image_pixel_budget` multiplies. Inert without it |
| `reference_video_frames` | `int` | `0` | no | — | 0 | — | Frame count of the clip being generated; a reference video is truncated to it before being presented, so that the encoder and the generator's own condition-encode cut it at the same frame. 0 (default) leaves a reference video untruncated. Inert without a `reference_video` input |
| `reference_selections` | `list` | `[]` | no | — | — | — | Per-request reference SUBSET, index-aligned with 'pairs' -- entry i is a list of indices into the packed 'reference_image'/'reference_video'/'reference_audio' order for output i. An absent or empty entry presents every packed reference (the default, and the only shape a non-Director ref2va request uses). The H3 adapter numbers '<Picture i>'/'<Video k>'/'<Audio j>' against whichever list it is actually handed, so a selection RE-LABELS that output's subset from 1 rather than keeping the packed set's own numbering. Inert without 'references' |

## prompt_expander

### <a id="pipe-prompt-expander"></a>`prompt_expander`

Expands prompts using configured LLM service

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `LLM` | `SERVICE` | yes | no | LLM service for prompt expansion |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `p_prompt` | `TEXT` | no | Expanded positive prompt |
| `n_prompt` | `TEXT` | no | Expanded negative prompt |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `llm_id` | `str` | — | no | — | — | — | LLM configuration ID |
| `command_id` | `str` | — | no | — | — | — | Predefined command ID |
| `style_id` | `str` | — | no | — | — | — | Prompt style ID |
| `prompt` | `str` | `"Expand this prompt"` | no | — | — | — | Expansion instruction |
| `p_prompt_input` | `str` | `""` | yes | — | — | — | Positive prompt to expand |
| `n_prompt_input` | `str` | `""` | no | — | — | — | Negative prompt to expand |
| `p_prompt_output` | `str` | `"[[__expanded_p_prompt__]]"` | no | — | — | — | Output template for positive |
| `n_prompt_output` | `str` | `"[[__expanded_n_prompt__]]"` | no | — | — | — | Output template for negative |

## sag

### <a id="pipe-sag-sdxl"></a>`sag/sdxl`

Self-Attention Guidance for SDXL (enhanced detail, ~15-20% overhead)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | SDXL model with hook registry |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `model` | `MODEL` | no | Model with SAG hook registered |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `scale` | `float` | `0.75` | no | — | 0.0 | 1.5 | SAG guidance strength |
| `sigma` | `float` | `2.0` | no | — | 0.5 | 10.0 | SAG blur sigma |
| `sag_threshold` | `float` | `1.0` | no | — | 0.0 | 5.0 | Attention-magnitude threshold for the blur mask (ComfyUI-parity; NOT a progress gate) |

## seed_generator

### <a id="pipe-seed-generator"></a>`seed_generator`

Pipe that will generate a seed

_No inputs._

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `seed` | `SEED` | yes | Generated random seeds for image generation |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `seed` | `int` | `-1` | no | — | — | — | Random seed for generation (-1 for random) |
| `quantity` | `int` | `1` | no | — | 1 | 20 | Number of seeds to generate |

## sharpness

### <a id="pipe-sharpness-sdxl"></a>`sharpness/sdxl`

Anisotropic sharpness enhancement for SDXL

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | SDXL model with hook registry |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `model` | `MODEL` | no | Model with sharpness hook registered |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `strength` | `float` | `0.0` | no | — | 0.0 | 30.0 | Sharpness strength (0=disabled) |

## tiled_detailer

### <a id="pipe-tiled-detailer-sdxl"></a>`tiled_detailer/sdxl`

SDXL-optimized tiled detailer for high-resolution image enhancement

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `model` | `MODEL` | yes | no | AI model for image generation |
| `conditioning` | `CONDITIONING` | yes | yes | Encoded prompt conditioning |
| `seed` | `SEED` | no | yes | Random seeds for generation |
| `image` | `IMAGE` | yes | yes | Input images to detail |
| `GPU` | `SERVICE` | no | no | GPU manager service for VRAM monitoring |
| `MEMORY` | `SERVICE` | no | no | Memory manager service for intelligent tiling |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Enhanced detailed images |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `tile_size` | `int` | `1536` | no | — | 256 | 4096 | Base tile size for processing |
| `overlap` | `int` | `128` | no | — | 16 | 512 | Overlap between tiles in pixels |
| `denoise` | `float` | `0.25` | no | — | 0.0 | 1.0 | Denoising strength |
| `steps` | `int` | `20` | no | — | 1 | 150 | Number of inference steps |
| `cfg` | `float` | `5.5` | no | — | 1.0 | 30.0 | CFG scale |
| `sampler` | `str` | `"DPMPP_2M"` | no | `EULER`, `EULER_A`, `HEUN`, `DPM2`, `DPM2_A`, `LMS`, `DPMPP_2S_A`, `DPMPP_SDE`, `DPMPP_2M`, `DPMPP_2M_SDE`, `DPMPP_3M_SDE`, `LCM` | — | — | Sampling algorithm to use |
| `scheduler` | `str` | `"karras"` | no | `normal`, `karras`, `exponential`, `sgm_uniform`, `simple`, `ddim_uniform` | — | — | Noise schedule type to use |
| `clip_skip` | `int` | `2` | no | — | 0 | 12 | Number of CLIP layers to skip |
| `seed` | `int` | `-1` | no | — | — | — | Random seed for generation (-1 for random) |
| `feather_amount` | `int` | `96` | no | — | 16 | 512 | Feathering amount for tile blending |
| `detail_boost` | `float` | `1.1` | no | — | 0.5 | 2.0 | Detail enhancement multiplier |
| `adaptive_tiles` | `bool` | `true` | no | — | — | — | Enable adaptive tile sizing |
| `enable_detail_conditioning` | `bool` | `true` | no | — | — | — | Enable detail-focused conditioning |
| `content_aware_denoise` | `bool` | `true` | no | — | — | — | Adjust denoise based on content complexity |
| `min_denoise` | `float` | `0.1` | no | — | 0.0 | 1.0 | Minimum denoise for uniform areas |
| `max_denoise` | `float` | `0.35` | no | — | 0.0 | 1.0 | Maximum denoise for detailed areas |
| `vram_limit_gb` | `int` | `32` | no | — | 4 | 80 | VRAM limit in GB for tile sizing |
| `min_tile_size` | `int` | `896` | no | — | 256 | 2048 | Minimum tile size to avoid tiny tiles |

## tiled_refiner

### <a id="pipe-tiled-refiner"></a>`tiled_refiner`

Upscale then refine an image tile-by-tile via low-denoise img2img (bounded VRAM)

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `image` | `IMAGE` | yes | yes | Images to upscale + refine |
| `model` | `MODEL` | yes | no | Native model bundle (reused from generation) |
| `conditioning` | `CONDITIONING` | yes | yes | Prompt conditioning reused per image |
| `seed` | `SEED` | no | yes | Per-image seeds |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Refined hi-res images |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `scale` | `float` | `2.0` | no | — | 1.0 | 8.0 | Target upscale factor |
| `upscale_model` | `str` | — | no | — | — | — | ESRGAN .pth under models/upscalers/ (blank -> Lanczos) |
| `denoise` | `float` | `0.25` | no | — | 0.0 | 1.0 | Per-tile img2img denoise strength (upper bound when content-aware) |
| `content_aware` | `bool` | `true` | no | — | — | — | Scale per-tile denoise by texture (flat tiles refine less; anti-hallucination) |
| `min_denoise` | `float` | `0.1` | no | — | 0.0 | 1.0 | Denoise floor for flat/low-texture tiles (content-aware) |
| `tile_size` | `int` | `1024` | no | — | 256 | 2048 | Tile working resolution (px) |
| `tile_overlap` | `int` | `128` | no | — | 0 | 512 | Tile overlap for feathered seams (px) |
| `steps` | `int` | `20` | no | — | 1 | 100 | Denoising steps per tile |
| `guidance` | `float` | `6.0` | no | — | 0.0 | 30.0 | CFG scale |
| `sampler` | `str` | `"euler"` | no | `euler`, `dpmpp_2m`, `unipc` | — | — | Sampler |
| `seed` | `int` | `-1` | no | — | -1 | — | Seed (-1 -> per-image random) |
| `device` | `str` | `"cuda"` | no | `cuda`, `cpu` | — | — | Compute device |

## upscaler

### <a id="pipe-upscaler"></a>`upscaler`

Tile-based RealESRGAN upscaling with user-defined scale

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `image` | `IMAGE` | yes | yes | Input images to upscale |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Upscaled images |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `model` | `str` | — | yes | — | — | — | Path to upscaler model (.pth file) |
| `scale` | `float` | `2.0` | no | — | 0.1 | 8.0 | Final upscaling factor |
| `tile_size` | `int` | `512` | no | — | 128 | 2048 | Size of tiles for processing |
| `tile_padding` | `int` | `32` | no | — | 0 | 128 | Padding between tiles for smooth blending |

## video_frame_extractor

### <a id="pipe-video-frame-extractor"></a>`video_frame_extractor`

Extract frames from video files at specified frame rate

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `video` | `VIDEO` | yes | yes | Input video files |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `image` | `IMAGE` | yes | Extracted video frames |
| `frame_metadata` | `DICT` | yes | Frame extraction metadata |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `frame_rate` | `float` | `1.0` | no | — | 0.1 | 60.0 | Frames per second to extract (e.g., 0.5 = 1 frame every 2 seconds) |
| `start_time` | `float` | `0.0` | no | — | 0.0 | — | Start time in seconds |
| `end_time` | `float` | `-1.0` | no | — | — | — | End time in seconds (-1 for full video) |
| `max_frames` | `int` | `-1` | no | — | — | — | Maximum number of frames to extract (-1 for no limit) |
| `frame_format` | `str` | `"png"` | no | `png`, `jpg`, `jpeg` | — | — | Output frame format |
| `quality` | `int` | `95` | no | — | 1 | 100 | JPEG quality (only used for jpg/jpeg format) |
| `frame_index` | `int` | — | no | — | — | — | When set, extract exactly this one frame (Python indexing, -1 = last) instead of sampling at frame_rate over the time range |

## video_frame_merger

### <a id="pipe-video-frame-merger"></a>`video_frame_merger`

Merge image frames into video files

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `image` | `IMAGE` | yes | yes | Input image frames |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `video` | `VIDEO` | yes | Generated video from frames |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `fps` | `float` | `30.0` | no | — | 1.0 | 240.0 | Output video frame rate |
| `codec` | `str` | `"mp4v"` | no | `mp4v`, `xvid`, `mjpg`, `divx` | — | — | Video codec |
| `output_format` | `str` | `"mp4"` | no | `mp4`, `avi`, `mov`, `webm` | — | — | Output video format |
| `loop_count` | `int` | `1` | no | — | 1 | 100 | Number of times to loop the frames |
| `reverse` | `bool` | `false` | no | — | — | — | Add reverse frames for ping-pong effect |
| `fade_in` | `int` | `0` | no | — | 0 | 60 | Fade in duration in frames |
| `fade_out` | `int` | `0` | no | — | 0 | 60 | Fade out duration in frames |
| `resize_mode` | `str` | `"keep"` | no | `keep`, `fit`, `fill`, `stretch` | — | — | How to handle frame resizing |
| `target_width` | `int` | `-1` | no | — | — | — | Target video width (-1 to use source) |
| `target_height` | `int` | `-1` | no | — | — | — | Target video height (-1 to use source) |

## video_speed

### <a id="pipe-video-speed"></a>`video_speed`

Change video playback speed using FFmpeg

**Inputs**

| Name | IO Type | Required | Array | Description |
|---|---|---|---|---|
| `video` | `VIDEO` | yes | yes | Input video file paths |

**Outputs**

| Name | IO Type | Array | Description |
|---|---|---|---|
| `video` | `VIDEO` | yes | Speed-adjusted video file paths |

**Configuration**

| Option | Type | Default | Required | Choices | Min | Max | Description |
|---|---|---|---|---|---|---|---|
| `speed_percent` | `int` | `50` | no | — | 10 | 100 | Playback speed as percentage of original (10=very slow, 100=original speed) |
| `keep_audio` | `bool` | `true` | no | — | — | — | Keep and adjust audio speed to match video |
| `output_format` | `str` | `"mp4"` | no | `mp4`, `mov`, `mkv`, `webm` | — | — | Output video format |
