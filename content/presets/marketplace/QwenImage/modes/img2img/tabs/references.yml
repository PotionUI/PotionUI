fields:
  - name: "source_image"
    type: "image"
    label: "Source Image"
    required: true
    ai_hint: "Upload the image to refine. It's snapped to the model's patch granularity before encoding."
